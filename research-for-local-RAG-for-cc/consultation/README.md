# consultation

このフォルダは、作業指示者が Claude Code と何らかの相談をする際に利用するフォルダ

## 相談時のフォルダ作成ルール

相談をする際には、本フォルダ直下に以下の命名規則に従ったサブフォルダを作成し、その配下に相談の内容を記述したファイルや、相談の中で生成するファイル群を配置する。

* **サブフォルダの命名規則:**  
  `<相談事項の通番(Cxx)>-<要旨が分かる端的な記述>-<相談の開始日(YYYYMMDD)>\`
  * 区切り文字は半角ハイフン `-` を使用する（要旨内に半角スペースが入る場合は半角アンダーバー `_` で代替する）
  * 通番 `Cxx` は本ファイルの「通番管理」節で一元管理する。新規相談着手時に次の番号を採番し、本ファイルの該当値を更新する
  * 通番 `Cxx-` は `improvements/` 配下の要改善点 ID 接頭辞（同じ `Cxx-yyy` 形式）と一致させ、相談 ↔ 改善点の紐付けを直接的に行う

### 通番管理

| 項目 | 値 |
|---|---|
| 次に利用する相談通番 | C03 |

### 相談中に複数のサブ作業（調査や議論・検討）が発生した場合のルール

サブ作業（ごとに、更にサブサブフォルダを作成し、その中で個々に作業する。  
ただし、サブ作業の内容が1つのファイルで完結する場合は、サブサブフォルダを作る方がコスト高になる可能性もあるので、これは厳守ルールはなく推奨ルール。  

* **サブサブフォルダを作成するか否かの判断基準と作成タイミング**
サブ作業内で生成するファイルが2つ以上になった際に、初めてサブサブフォルダを作成する。

* **サブサブフォルダの命名規則:**  
  `<サブ論点の議論の通番+要旨が分かる端的な記述>\`

## 変更履歴

- 2026-05-10: 初版作成(作業指示者による)
- 2026-05-10: サブフォルダ命名規則を `<Cxx>-<要旨>-<YYYYMMDD>` 形式に変更（improvements/ の `Cxx-yyy` 改善点 ID と直接紐付け可能化、同日複数相談時の一意性確保）。通番管理節を追加
