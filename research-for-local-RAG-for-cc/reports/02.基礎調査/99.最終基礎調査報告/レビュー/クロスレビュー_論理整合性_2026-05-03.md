# クロスレビュー — 論理整合性観点

## メタ情報

| 項目 | 内容 |
|---|---|
| レビュアー | Claude Opus 4.7（独立第三者） |
| 観点 | 論理整合性・カバレッジ十分性・調査目的との整合性・仮説の妥当性 |
| レビュー対象 | `C:\cc-workspace\research-by-cc\research-for-local-RAG-for-cc\reports\02.基礎調査\99.最終基礎調査報告\報告書.md` |
| 判断基準 | 作業計画書 v1.0 §4 / §5.2 / §5.4、`CLAUDE.md`（research-for-local-RAG-for-cc）、02.基礎調査 サブタスク 01〜08 各報告書、`ハマりどころ.md` |
| レビュー日 | 2026-05-03 |
| スコープ外 | 完了条件の明瞭性そのもの・命名規則・文書構造・検証パターンの具体性（実用性レビュアー担当） |

---

## レビュー結果サマリ

| ラベル | 件数 |
|---|---|
| CRITICAL | 1 |
| IMPORTANT | 4 |
| SUGGESTION | 3 |
| POSITIVE | 3 |

---

## CRITICAL

### C1. H1 仮説の「現時点ステータス」評価が、計画書の H1 命題に対する判定として未解決のまま残っている

**問題点**:

計画書 §4 の H1 は「コードベース系データには Filesystem Exploration RAG（grep/glob）の方がベクトル検索より精度・速度で有利」という明確な命題である。

99 報告書 §2.3 / §4 H1 では、現時点ステータスを「**対立軸が補正された**（grep/glob 単独 vs 汎用ベクトルではなく、コードグラフ+シンボル検索 vs 汎用ベクトル）」と記載しており、H1 そのものへの判定（成立 / 不成立 / 部分成立 / 修正後再評価）が明示的に下されていない。サブタスク 07 §4 では Codebase-Memory 論文（2026-03）を引用して「コードグラフ系は file-explorer 比でトークン 1/10、ツール呼び出し 2.1 倍削減、回答品質 83%（**file-explorer: 92%**）」と報告しており、この数値は **「精度（回答品質）では file-explorer が 9 ポイント優位」** という H1 成立寄りの根拠と、**「トークン効率では grep/glob が劣後」** という H1 不成立寄りの根拠の両方を示している。

「対立軸補正」は調査の進展としては正しい記述だが、計画書で立てた H1 命題に対しては:

- 精度面: H1（grep/glob 優位）寄りに支持あり（file-explorer 92% vs Codebase-Memory 83%）
- 速度面: H1 寄り（grep <0.02s）
- トークン効率: **H1 と逆方向**（コードグラフ系がトークン 1/10）

という多面的判定がサブタスク 07 から導けるはずである。99 報告書ではこの判定を「対立軸補正済み」の一語で済ませており、計画書 §4 で立てた H1 命題（精度・速度の両軸での優劣）への明確な回答がない。

これは「02.99 完了条件: 各仮説（H0〜H5）の現時点の検証状況が整理されていること」（計画書 §5.2）に対するカバレッジ欠落に該当する。下流の 03 設計フェーズで H1 を引き継ぐ際、「H1 の真偽は何だったのか」が判定不能となり、検証対象選定（特に Filesystem Exploration を必須対照群として残す根拠）の論理的トレーサビリティが断たれる。

**修正案**:

§2.3 / §4 H1 のステータスを以下のような多面判定に書き換える:

- **H1 精度面**: 部分成立（コードベースデータでは file-explorer 系が Codebase-Memory 系を 9 pt 上回る／GrepRAG では Vanilla RAG を 54% 上回る）
- **H1 速度面**: 成立（grep p50 <0.02s vs ベクトル ~30ms）
- **H1 トークン効率面**: 不成立（grep/glob は線形増加、コードグラフ系は 1/10）
- **対立軸の補正**: 元の二択（grep/glob vs 汎用ベクトル）は不適切と判明し、04 では「Filesystem Exploration（H1 検証用必須対照群）」「コードグラフ系（新規追加候補）」「汎用ベクトル」の 3 軸比較が必要

この多面判定により、H1 が単純な真偽ではなく評価軸ごとに異なる結論を持つことを下流フェーズに正確に引き継げる。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

## IMPORTANT

### I1. 「mcp-local-rag (shinpr)」の根拠記述が薄く、エグゼクティブサマリでの「最有力」評価の論理的支えが弱い

**問題点**:

99 報告書 §2.1（採用候補表）では「既製 OSS（最有力・MCP 汎用 RAG）」として **mcp-local-rag（shinpr）** を第一候補に挙げ、§3.6 / §3.8.2 でも「軽量（90MB 埋め込みモデル）、`npx` で即利用可能」と記述、§6.2 では「03.02 で検証対象追加要否を判断」とまで踏み込んでいる。

しかしサブタスク 06 報告書を確認すると、§7「採用候補の整理」表に突如「mcp-local-rag (shinpr)」が「A（最有力）」として現れているのみで、§4 の参考記事 5 本（記事1〜5）の本文中には shinpr/mcp-local-rag への言及がない。「90MB 埋め込みモデル」「`npx` で即利用可能」という具体的なスペック情報の出典が報告書内では確認できず、**Web で直接調査した結果が §7 表のみに反映されたが、調査経緯・出典が報告書本文に記載されていない**可能性が高い。

サブタスク 08 でも本リポジトリは調査対象外（CLAUDE.md 参照リソース 8 件には含まれない）。99 報告書はこの根拠不足を引き継いだまま、「最有力」「03.02 で検証対象追加検討」と書き下ろしている。

これは「採用候補の根拠の論理的トレーサビリティ」の観点で、03 設計フェーズが「なぜ mcp-local-rag が最有力なのか」を遡って確認できない状態を生む。

**修正案**:

以下のいずれかで対応する:

1. サブタスク 06 報告書の §7 採用候補表で「mcp-local-rag (shinpr)」の出典（GitHub URL・README の実機能・90MB の根拠など）を補完するよう、サブタスク 06 報告書の改訂を 02.99 から指示する（FB ループ起動）
2. 99 報告書 §3.6 / §3.8.2 に「2026-05-03 時点では shinpr/mcp-local-rag の詳細スペック（90MB の根拠等）はサブタスク 06 内での Web 調査結果に依拠しており、03.02 の検証対象追加判断時に動作確認を含めた再評価が必要」と注記を追加し、暫定的最有力扱いであることを明示する
3. 「最有力」表現を「候補（要詳細確認）」に格下げする

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

### I2. H2「CLI 起動 8〜9 秒の遅延が実測値として確定」は単一構成・単一記事の参照値であり「確定」表現が過剰

**問題点**:

99 報告書 §2.3 / §4 H2 で「**CLI 起動 8〜9 秒の遅延が実測値として確定**」と表現しているが、サブタスク 06 報告書 §4 記事3（karaage0703）の値は以下のように明確に限定された前提下の値である:

- 構成: `multilingual-e5-small` + Skills (CLI 都度起動) + DuckDB
- モデルサイズ: 約 100〜500 MB（小型モデル）
- 報告者: 単一記事（karaage0703 1 名）
- DB 操作部分: 約 1 秒（DB 規模・クエリ複雑度に依存）

99 報告書の採用候補は **bge-m3（4.6 GB）** が第一候補であり、この場合の CLI 都度起動時のモデルロード時間は **8 秒では収まらない可能性が極めて高い**（モデルサイズが約 9 倍違う）。「8〜9 秒の遅延が確定」という表現は、本ユースケースの第一候補構成に当てはまらず、誤解を招く。

下流フェーズで「CLI 都度起動 = 8〜9 秒」という値を独立した前提として参照すると、bge-m3 採用時の H2 検証で実測値が大きく異なり、検証設計の前提が崩れる懸念がある。

**修正案**:

§2.3 / §4 H2 の表現を以下のように補正する:

- 「CLI 起動 8〜9 秒の遅延が実測値として確定」 → 「CLI 都度起動型のモデルロード遅延が顕著（軽量モデル `multilingual-e5-small` 構成で 8〜9 秒の実測値、bge-m3 等の大型モデルでは更に増加見込み）として確認」
- 04 での扱いに「採用候補の埋め込みモデル（bge-m3 / ruri-v3 等）ごとの実測ロード時間を計測する」を追加

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

### I3. TBD3「解消」と「03.01 で再確定」の二重表現が論理的に矛盾している

**問題点**:

99 報告書 §2.4 残 TBD ステータス表で TBD3（規模感）を「**解消**（数 MB〜数百 MB、ファイル数百〜数万件…）」と評価する一方、同じ行の「次フェーズでの確定方針」列で「**03.01 でユースケース別の代表サンプルとして規模を再確定**」と記述している。§5 TBD3 でも同じ二重表現がある。

「解消」と「再確定」は同時に成立しない。実際にサブタスク 01 §4 を確認すると、規模感は「本調査の想定規模は数MB〜数百MBオーダー、ファイル数は数百〜数万件（前回サマリー通り確認）」とあり、これは **代表ユースケースから導いた値ではなく、調査着手時の仮想定オーダー** である。計画書 §4 TBD3 の検証方法は「文献調査による『探索空間の閾値』『CAG / RAG / Filesystem Exploration の境界』の特定」であり、これは満たしているが、計画書 §5.3.1 の 03.01 完了条件「TBD3（データ規模）が代表サンプル選定により概算値として記入されていること」とは別の解像度の作業である。

つまり TBD3 は **「探索空間境界としての規模感は解消（02.01）」「代表ユースケースの個別値としての規模は 03.01 で確定」** という 2 つの粒度に分けて記述するべきで、「解消」だけでは下流の 03.01 担当が「もう確定済みで再作業不要」と誤解する余地を残す。

**修正案**:

TBD3 のステータスを以下のように 2 段階で記述する:

- **TBD3-a（探索空間境界の規模感）**: 解消（02.01 で文献ベース概算オーダー確定）
- **TBD3-b（代表ユースケース別の規模値）**: 03.01 で確定予定（計画書 §5.3.1 完了条件）

または「**部分解消**（探索空間境界の概算オーダーは確定。代表ユースケース別値は 03.01 で確定）」と表現する。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

### I4. TBD2「解消（スコープ外確定）」とコードグラフ系のスコープ内採用が、TBD2 の問い設定との関係で論理的に整理不足

**問題点**:

計画書 §4 TBD2 の問いは「**Graph RAG の本ユースケース適用可能性**（脆弱性パッチ・コード関連グラフ等）」。

99 報告書 §2.4 / §5 TBD2 では「**解消（スコープ外確定）**」と記述しつつ、§5 TBD2 の補足で「**ただし、コードベース向けの『コードグラフ系』（Codebase-Memory MCP / isink17/codegraph）はローカル LLM 不要のため別カテゴリとしてスコープ内**」と書き加えている。

これは論理的に「Graph RAG は何を指していたのか」の定義問題に行き当たる。サブタスク 01 §6 の TBD2 結論では「Graph RAG（KG構築フェーズで LLM 必須なツール群: Microsoft GraphRAG / LightRAG / Neo4j+LangChain）はスコープ外」と限定して書いており、これは「LLM 必須ツール群 = Graph RAG」という暗黙の定義に立っている。

しかしサブタスク 07 §2.2 / §4 では「コードグラフ系（Codebase-Memory MCP / isink17/codegraph / GitNexus / Code-Graph-RAG）」も含めて「コードベース版の Graph RAG 的アプローチ」として扱っており、Graph RAG の定義範囲が広がる。

99 報告書はサブタスク 01 の狭義 Graph RAG（既製ツール群）を「スコープ外」、サブタスク 07 の広義コードグラフ系を「スコープ内」として両立させようとしているが、TBD2 の問い「Graph RAG の本ユースケース適用可能性」に対しては「**狭義: スコープ外、広義: スコープ内（コードグラフ系として）**」という二分判定が論理的にあるべき。「解消」という単語だけでは不十分。

**修正案**:

§5 TBD2 のステータスを以下のように再整理する:

- **狭義 Graph RAG（KG 自動構築に LLM 必須の既製ツール: GraphRAG / LightRAG / Graphiti 等）**: スコープ外確定
- **広義コードグラフ系（KG 構築が AST/シンボル解析ベースで LLM 不要: Codebase-Memory MCP / isink17/codegraph 等）**: スコープ内（§3.7 参照）。03 設計フェーズで検証対象追加検討
- **「擬似 Graph RAG」（Claude Code 自身が JSON で依存関係を構築）**: 構築・維持コスト高のため当面除外、申し送り（§8.4）

これにより TBD2 の問い「Graph RAG の本ユースケース適用可能性」への論理的に完結した回答となる。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

## SUGGESTION

### S1. §2.1 採用候補表「BM25 日本語処理」の代替候補に MeCab を挙げた根拠が弱い

**観点**:

論理的根拠の整合性。

§2.1 採用候補表で「BM25 日本語処理」第一候補に「rank_bm25 + sudachipy」、補完／代替候補に「MeCab（精度比較対象）」と記載している。しかしサブタスク 05 報告書では:

- §7 採用候補の整理表（推奨構成）で「BM25: `rank_bm25` + `sudachipy`」のみ記載、MeCab への言及なし
- §6 TBD3 依存部分の「BM25 日本語トークナイザー選択」で「Sudachi（`sudachipy`）を第一候補」「実際の日本語テキスト品質・固有表現頻度で MeCab との比較検証が必要」と「将来検証必要」レベル
- §8 課題・制約「sudachipy と MeCab の精度・速度比較を実ファイルで検証する必要がある」

つまり MeCab は「将来検証候補」レベルで、99 報告書のように「補完／代替候補」として並列に並べるには根拠が弱い。

**提案**:

§2.1 表の「補完／代替候補」欄を「MeCab（将来検証対象、現時点では sudachipy のみ採用候補）」に修正するか、欄から削除して脚注または §6.2 設計判断ポイントへの参照とする。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

### S2. §2.4 TBD4 / TBD5「未着手」の評価は計画書 §10 と整合させて表現を改善できる

**観点**:

計画書との整合性。

§2.4 TBD4「未着手」「03.02 で枠組みと暫定値を定義、05.01 で最終確定」、TBD5「未着手」「03.02 で確定」と記載。

計画書 §4 / §10 では TBD4 は「03-02」、TBD5 は「03-02」が確定タイミングと記載されており、02.99 完了時点で「未着手」と書くのは正しい。ただし計画書 §10 「次の見直しポイント」表では「TBD4（指標枠組み）/ TBD5（検証対象数）の確定」が「03.ローカル RAG 設計 完了時」のトリガーと位置付けられており、「未着手」より「**計画通り 03.02 で確定予定（02 段階では着手不要）**」の方が、論理的に正確で「進捗遅延ではない」ことが明示できる。

**提案**:

「未着手」を「**計画通り 03.02 で確定予定（02 段階では着手不要）**」に変更する。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

### S3. §3.2 ベクトル DB 候補の選定順位の判断基準を明示すると下流の引き継ぎが容易になる

**観点**:

論理的判断基準のトレーサビリティ。

§3.2 で第一〜第四候補（DuckDB+vss / LanceDB / sqlite-vec / Chroma）を順位付けしているが、各候補の選定根拠は個別の特長記述に分散しており、「**なぜ DuckDB+vss が第一なのか**（LanceDB と比べて）」「**規模・次元数・Windows 対応のどの軸で順位が決まったのか**」が一目で読めない。

サブタスク 03 §3.1 では「第一候補: DuckDB + vss」「第二候補: LanceDB」「第三候補: sqlite-vec」「第四候補: Chroma」の順位は同じだが、サブタスク 03 §5 「現時点での暫定推奨」では「Claude Code の利用規模（数千〜数十万チャンク程度）を想定すると、DuckDB + vss または LanceDB が妥当」と DuckDB と LanceDB が同列扱い。

99 報告書では順位差が固定化されているため、「DuckDB と LanceDB を分ける論理基準」を §3.2 内に補強できると、03.02 設計時の検証対象選定で議論しやすくなる。

**提案**:

§3.2 の冒頭に「順位付けの判断基準: (1) 単一ファイル型優先、(2) Windows 公式サポート、(3) 既存事例実証、(4) 多言語次元対応の順で評価」のような選定基準サマリを 1〜2 行追加する。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （採用 / 却下 / 保留 / 改変） |  |

---

## POSITIVE

### P1. 除外候補（§2.2）の根拠が各サブタスク報告書と整合し、論理的に明確

**評価点**:

§2.2 除外候補表は 11 件の除外対象に対し、それぞれ「ライセンス（CC-BY-NC / Gemma 独自）」「OS 非対応（Milvus Lite Windows、Sourcegraph 系外部依存）」「コンテキスト長制限（128 トークン）」「機能不足（RAG なし）」「環境制約（Docker 必須）」など、明確かつ独立した根拠を 1 行で示している。

サブタスク 02 §3.2、サブタスク 03 §3.2、サブタスク 05 §4、サブタスク 07 §5.2、サブタスク 08 §3.3 の各除外判定とすべて整合しており、除外決定の論理的トレーサビリティが完璧に保たれている。03 設計フェーズでこの除外候補リストを引き継ぐ際、「なぜ採用しなかったか」の再議論が不要になる。

CLAUDE.md「大前提（LLM でなくてもできることはCCにさせない）」と「OSS かつ商用利用無料」の制約に照らし、各除外根拠が直接対応している点も論理整合性として優れている。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （維持 / その他） |  |

---

### P2. H1 軸補正の発見をサブタスク 07 から正しく汲み取り、03 設計フェーズへの具体的入力（§6.2）として再構成している

**評価点**:

サブタスク 07 §4「H1 への直接回答」で示された「**コードグラフ + シンボル検索（AST・グラフ・grep を統合したコード特化構成）vs 汎用ベクトル RAG**」という対立軸の補正を、99 報告書では:

- §3.7「H1 軸の補正（重要）」として明示
- §4 H1 の現時点ステータスとして反映
- §5 TBD2 補足としてコードグラフ系のスコープ位置付けを明確化
- §6.2 設計フェーズへの入力で「コードグラフ系の検証対象追加要否」として具体的判断ポイントに変換
- §7.3 計画書側への改訂提案として「§4 仮説テーブル H1」改訂候補に明記

という多層的なフォローアップを行っており、新発見の調査からの設計入力への変換が論理的に完結している。基礎調査内 FB ループ（計画書 §5.2 末）の意図に正確に沿った成果物となっている。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （維持 / その他） |  |

---

### P3. §8 課題・制約の 4 分類（将来解決必要 / 解決不能 / 解決不要 / 申し送り）と各エントリの粒度が CLAUDE.md 共通完了条件に整合し、根拠も明確

**評価点**:

§8 では計 30 件以上の課題・制約を「将来解決必要 / 解決不能 / 解決不要 / 申し送り」の 4 分類で整理しており、CLAUDE.md および計画書 §5.2 完了条件「課題・制約が末尾に分類して記載されていること」を満たすだけでなく、各エントリで:

- 元のサブタスクとのトレーサビリティ（02.01 / 02.02 / 02.06 / 02.07 / 02.08 由来が個別に追跡可能）
- 具体的根拠（モデルサイズ、ライセンス情報、Go バージョン要件等）
- 解決不能と判定する論理的根拠（「ローカル LLM 不使用の制約下では…代替手段がない」「ベクトル類似度は本質的にコードの依存構造を表現できない」など）

を一貫して記述している。特に「解決不要」分類でも「採用しないと確定済み」「OSS 代替で代替可能」と判定根拠を明示しており、03 以降のフェーズで再議論せずに済む論理的完結性を持つ。

**作業指示者の態度表明**:

| 態度 | コメント |
|---|---|
| （維持 / その他） |  |

---

## 変更履歴

- 2026-05-03: 初版作成（Claude Opus 4.7、独立第三者レビュアー）
