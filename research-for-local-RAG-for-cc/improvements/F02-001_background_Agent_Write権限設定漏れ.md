# F02-001 background Agent の Write 権限設定漏れによる処理中断・再実行

## 1. はじめに

### 1.1 要改善点概要

フェーズ 02（基礎調査）の並列 Agent 実行（パターン A：Sonnet 8 並列）において、background Agent がファイルへの書き込み時に権限プロンプト（permission prompt）を受け取れず Write が拒否され、処理が中断した。事前に `settings.json` の `permissions.allow` へ `Write` 権限を追加しておく手順が抜けていたことが直接原因。事象解消のため処理中断後に権限を追加し再実行する対応（手戻り）が発生した。

### 1.2 サマリ

| 項目 | 内容 |
| --: | :-- |
| **カテゴリ** | ツール／権限 |
| **優先度** | 高 |
| **スコープ** | 横断 |
| **ステータス** | 未対処 |
| **影響範囲** | background Agent がファイル書き込みを行う全フェーズ・全サブタスク。特に複数 Agent を並列起動するパターン A／C が高リスク |
| **関連 ID** | なし |
| **採用案** | — |

## 2. 発生事象の整理

### 2.1 発生事象

2026-05-03（日）のフェーズ 02 基礎調査において、作業計画書 v1.0 に定義された「パターン A：並列（Sonnet 8 並列）」で 8 本の background Agent を同時起動し、各サブタスクの調査報告書を書き込む作業を行った。このとき、background Agent は Claude Code のメインセッションから切り離されて動作するため、実行中に権限プロンプトを受け取ることができない。一方、実行時点の `settings.json`（コミット `a463ec6` 以降の初期状態）の `permissions.allow` リストには `Write` 権限が含まれていなかった。その結果、各 Agent が調査報告書ファイルを書き込む段階で Write が拒否され、処理が中断した。

事象解消のため、以下の対応が発生した:
1. `settings.json` の `permissions.allow` に `Write(research-for-local-RAG-for-cc/**)` を追加（コミット `a756e7e`）
2. background Agent を再実行（コミット `51211f5`）

### 2.2 発生経緯

1. **計画フェーズ**: 作業計画書 v1.0（コミット `6711737`、2026-05-02）でフェーズ 02 の実施方針として「パターン A：並列（独立サブタスクを並列実行）、機械的情報抽出・要約（Sonnet）」が決定された。計画書には並列 Agent の起動方式や役割が明記されていたが、background Agent が Write 操作を行うための事前権限設定という手順は言及されていなかった。

2. **実行フェーズ**: 2026-05-03 にフェーズ 02 の実作業に着手し、Sonnet 8 並列で background Agent を起動した。各 Agent が `research-for-local-RAG-for-cc/reports/02.基礎調査/` 配下に調査報告書ファイルを書き込もうとした段階で Write が拒否され、処理が中断した。

3. **事後対応**: 処理中断を受けて `settings.json` に Write 権限を追加し（コミット `a756e7e`、コミットメッセージ: "02.基礎調査の並列 Agent 実行時に、background agent が permission prompt を受けられず Write が拒否される問題を解消するための事前許可"）、再実行した（コミット `51211f5`）。

4. **フェーズ 02 振り返り**: 2026-05-04 の振り返りで本事象が要改善点として識別され F02-001 として起票された。`improvements/` フォルダ新設時のコミット（`6f0080c`）では `Write(research-for-*/**)` および `Edit(research-for-*/**)` へのグロブ化拡張が「F02-001 対応」として実施された。

## 3. 原因分析

### 3.1 直接原因

- **事前の Write 権限の設定漏れ**: background Agent を起動する前に `settings.json` の `permissions.allow` に `Write` 権限を追加する手順が実行されなかった。結果として、background Agent がファイルを書き込もうとした際に Claude Code の権限チェックで拒否され、処理が中断した。

### 3.2 根本原因

## なぜなぜ分析: background Agent の Write 権限設定漏れによる処理中断

### 問題記述

**What（何が）:** フェーズ 02 の background Agent（Sonnet 8 並列）がファイル書き込み時に Write 権限拒否を受け、処理が中断した  
**When（いつ）:** 2026-05-03、フェーズ 02 基礎調査の並列 Agent 実行時  
**Where（どこで）:** `settings.json` の `permissions.allow` と、background Agent の Write 操作（`research-for-local-RAG-for-cc/reports/02.基礎調査/` 配下）  
**Impact（影響）:** 8 本の Agent 処理が中断し、Write 権限追加 → 再実行という手戻りが発生。作業時間ロスおよびコスト（トークン）の再消費が生じた

### Why Chain

| Why | 問い | 答え | 根拠・証拠 |
|---|---|---|---|
| Why 1 | なぜ background Agent が Write を拒否されたか？ | 実行時点の `settings.json` の `permissions.allow` に `Write` エントリが存在しなかったため | コミット `a463ec6`（初期化）〜`a756e7e`（修正）の間の `settings.json` には `Write` エントリが無い。`a756e7e` のコミットメッセージが "permission prompt を受けられず Write が拒否される問題を解消" と明記している |
| Why 2 | なぜ `permissions.allow` に `Write` が追加されていなかったか？ | background Agent を使う作業を計画・開始する時点で、権限設定の準備チェックを行う手順が存在しなかったため | 作業計画書 v1.0（コミット `6711737`）にはパターン A 並列の実施方針は記載されているが、事前権限確認手順への言及はない。`ハマりどころ.md` にも Write 権限確認に関するエントリは存在しない（2026-05-06 時点での全文検索で確認） |
| Why 3 | なぜ権限設定の準備チェック手順が存在しなかったか？ | background Agent が「メインセッションと異なるパーミッション環境で動作し、実行中に権限プロンプトを受け取れない」という特性が、計画・設定レビューのチェック観点として明文化されていなかったため | 作業計画書・CLAUDE.md（いずれも初期コミット `a463ec6` 時点）のいずれにも、background Agent 固有の権限制約とその事前準備に関する記述が存在しない。この特性は実際に処理中断が発生して初めて認識された（コミット `a756e7e` が初出） |
| Why 4 | なぜ background Agent の権限制約が事前に共有・明文化されていなかったか？ | 「background Agent を並列大量起動する」作業パターンが本プロジェクトで初めて実施されたため、権限設定の落とし穴が既知の知識として蓄積されていなかった。また、設定ファイル（`settings.json`）の用途と background Agent の動作制約との関係を、作業着手前に確認するプロセスが設計されていなかったため | コミット `a463ec6`（初期化）は 2026-05-01 で、background Agent を使った本格的な並列実行はコミット `51211f5`（2026-05-03）が初回。初回実施前に類似事例や公式ドキュメントを参照して権限制約を確認するステップが、計画書上にも CLAUDE.md 上にも設定されていなかった |

### 根本原因

**特定した原因:** 「background Agent 固有の権限制約（実行中に permission prompt を受け取れないため、事前に `permissions.allow` への登録が必要）」が、作業パターン（並列 Agent）の計画・準備フェーズにおける確認観点として明文化されておらず、初回実施時に処理中断という形で初めて発見された

**種別:** プロセス（作業着手前の準備チェックリスト／知識共有の欠如）

**確信度:** 高

### バリデーション（逆方向確認）

- 「background Agent 固有の権限制約の明文化と事前準備チェックの組み込み」が解消 →「権限設定の準備チェック手順が存在しない」（Why 3）が防止される ✓
- Why 3 が防止 → 「`permissions.allow` に Write が追加されていなかった」（Why 2）が防止される ✓
- Why 2 が防止 → 「background Agent が Write を拒否された」（Why 1）が防止される ✓
- Why 1 が防止 → 「処理中断・再実行」（問題）が発生しない ✓

チェーンは成立する。

### 対策（5-whys スキル出力）

| 種別 | 対応内容 | 担当 | 期限 |
|---|---|---|---|
| 即時対応 | `settings.json` の `permissions.allow` に `Write(research-for-*/**)` と `Edit(research-for-*/**)` を事前追加（コミット `a756e7e` および `6f0080c` で実施済み） | — | 対応済み（2026-05-03〜04） |
| 予防措置 | background Agent を使う作業開始前に権限設定を確認するチェック観点を `ハマりどころ.md`・CLAUDE.md・`settings.json` 用意手順のいずれかに追加 | Claude / 作業指示者 | 次期 Agent 並列実行前 |
| 検出 | 新規プロジェクト（`research-for-xxx/`）開始時に `settings.json` テンプレートへ `Write(research-for-*/**)` を既定エントリとして含める | Claude / 作業指示者 | 次期プロジェクト開始時 |

## 4. 同件調査

### 本プロジェクト内の同件・類件調査

**調査範囲:** `research-for-local-RAG-for-cc/` 配下の全フェーズ、および git log 全体

**結果:**

- **フェーズ 02 以降の同件発生**: フェーズ 02 以降のコミット（コミット `6f0080c` で `Write(research-for-*/**)` に拡張後）では Write 権限拒否に起因するコミットメッセージや振り返りの記載は確認できない。拡張後の設定が機能していると判断できる。
- **類件（Edit 権限）**: コミット `6f0080c` では Write に加えて `Edit(research-for-*/**)` も同時追加されており（コミットメッセージ："settings.json の Write/Edit 権限をグロブ化し"）、Edit 権限も同様の懸念があったことが示唆される。Edit 単独での処理中断事象は git log からは確認できないが、事前的な対応として同時追加されたと解釈できる。
- **`ハマりどころ.md` への記録**: background Agent の Write 権限設定に関するエントリは `ハマりどころ.md` に記載されていない（2026-05-06 時点での全文検索で確認）。知識として共有されていない状態であり、新規フェーズ・新規プロジェクトで再発するリスクが残存している。

### 他プロジェクト（research-for-xxx）の調査

本ワークスペース内の現時点での調査プロジェクトは `research-for-local-RAG-for-cc/` のみであり、他の `research-for-xxx/` フォルダは存在しない（2026-05-06 時点）。ルート `settings.json`（コミット `6f0080c` 以降）のグロブ `Write(research-for-*/**)` は全プロジェクトに適用されるため、次期プロジェクトで同じ Write 拒否は発生しない見込みであるが、ルールとしての明文化は未着手のため、設定ファイルを初期化し直す場面では再発しうる。

## 5. 再発防止策

| # | 対応区分 | 推奨 | 内容 | Pros. | Cons. |
| -- | -- | -- | :-- | :-- | :-- |
| 1 | 根本 | 推奨 | `ハマりどころ.md`（または CLAUDE.md）に「background Agent を並列起動する場合、事前に `settings.json` の `permissions.allow` に必要な Write・Edit・MCP ツール権限を追加する」旨のエントリを追加し、各フェーズ作業開始時の必須参照事項とする | 既存の「各フェーズ作業の開始時に必ず `ハマりどころ.md` を参照する」運用ルール（CLAUDE.md §ハマりどころ.md 参照ルール）に乗るため、追加の行動変容を不要とする。横断スコープの課題なので `ハマりどころ.md` よりルート CLAUDE.md または `.claude/rules/` への昇格が有効 | `ハマりどころ.md` への記載のみでは、新規プロジェクト開始時に引き継がれない（プロジェクト固有ファイルのため） |
| 2 | 根本 | - | ルート CLAUDE.md の「Agent 活用ガイドライン」節またはルート `.claude/rules/` に「background Agent 使用前チェックリスト」として、Write / Edit / MCP ツールの権限事前確認を明記する | ルート CLAUDE.md は全プロジェクト共通でロードされるため、新規プロジェクトにも漏れなく適用される | ルート CLAUDE.md の変更は全プロジェクトに影響するため、作業指示者のレビュー・承認が必要 |
| 3 | 根本 | - | ルート `.claude/settings.json` のテンプレート解説コメントに「background Agent を使うフォルダを `Write(research-for-*/**)` で事前登録するパターン例」を示す | 設定ファイル自体をドキュメントとして機能させられる | JSON にコメントを書けないため、別ドキュメント（CLAUDE.md や README）を参照させる間接的な方法に限られる |
| 4 | 暫定 | 推奨 | 現在のルート `settings.json` のグロブ設定（`Write(research-for-*/**)` / `Edit(research-for-*/**)`）を維持し、新規プロジェクト開始時にこの設定が適切に引き継がれているかを確認する習慣を作業指示者に周知する | コミット `6f0080c` で既に対応済み（グロブ化）のため即効性が高く、フェーズ 03 以降はすぐに恩恵を受けられる | 「設定を維持する」というルールが明文化されていないため、将来的な settings.json のリファクタリング時に誤って削除されるリスクが残る |
| 5 | 暫定 | - | 新規プロジェクト（`research-for-xxx/`）の `CLAUDE.md` 雛型に「background Agent 使用前に `settings.json` の Write/Edit 権限を確認すること」という注記を追加する | プロジェクト固有 CLAUDE.md に記載することで、そのプロジェクト内では自己完結した確認が可能になる | 毎回人手で注記を入れる必要があり、雛型自体の管理コストが増える |

## 6. 特記事項/補足事項

- コミット `6f0080c` の対応（`Write(research-for-*/**)` のグロブ化と `Edit(research-for-*/**)` の追加）は、フェーズ 02 振り返りで「F02-001 対応」と明記された暫定修正であり、根本的な防止策（明文化・チェックリスト化）は未着手のまま。
- background Agent が `permissions.allow` なしに権限プロンプトを受け取れない制約は Claude Code の仕様であり、変更できない。再発防止はプロセス側（事前確認の仕組み化）でのみ実現可能。
- `Edit` 権限についても同様の懸念があるが、今回の事象で直接確認された Write 拒否と同一の根本原因を持つため、同一チケットで管理する。
- §4 の調査より、`ハマりどころ.md` にこの落とし穴が未記載であることが判明した。§5-1（推奨）の実施時に合わせて追記することを推奨する。

---

## 変更履歴

- 2026-05-06: 初版作成（Claude Sonnet 4.6、5-whys スキル適用）
