# ポータブルな `.claude/` のチーム共有・統制 — 結論・構成案（v1.1）

> - **想定読者**: Claude Code を活用するチームのガバナンス担当者。`<Prjルート>\.claude` をチームメンバーへ配布するベストプラクティス（最適な手段と、それを実現する構成・リポジトリ設計）を知りたい読み手。
> - **位置づけ**: 確定成果物（v1.0）。`.claude/workspace/` での調査（初版〜v2.6）と3観点クロスレビューを経て確定し、reports/ へ昇格・v1.0 として再採番した版。
> - **作成日**: 2026-06-06（調査着手）／ **v1.0 確定**: 2026-06-07
> - **根拠ドキュメント**: Claude Code 公式ドキュメント（snapshot = CLI v2.1.165 と一致、changelog 先頭 = v2.1.165）。本文中の各主張には `[n]` の形でアンカーを付し、末尾の[§出典一覧](#sources)へリンクする。
>
>   - 本文: `cc-relative-info\LLMs\official-llms-txts\code.claude.com\docs\llms-full.txt`（63,719 行。出典一覧の行番号はこのファイルに対応）
>   - 差分サマリ: `cc-relative-info\LLMs\official-doc-update-summary\claude-code-docs\`（2026-05-31〜06-06）
>   - 精読の根拠: 調査時の 7 Agent ページ別精読（`.claude/workspace/portable-claude-dir-sharing/intermediate-reports/01〜07-*.md` に保管）
>   - クロスレビュー: [`./レビュー/`](./レビュー/)（論理整合性・実用性・作業指示者+人間読み手の3観点・各1.0版）

---

## 問い

1. `<Prjルート>\.claude` を Marketplace の仕組みで配布できるか。
2. 特定リポジトリ向けにチューニングされていない、ゼロから作ったリポジトリでも使える **最低限の資産だけのシンプルな `.claude/`** を、Marketplace のように Skill 等を配布する形でチーム共有するベストプラクティスは存在するか。
   存在しなければ、ポータブルな `.claude/` をチーム共有・統制するための最適な仕組みを設計せよ。

---

<a id="summary"></a>

## エグゼクティブサマリ（結論の先出し）

> 急ぐ読者はこの節だけで結論を把握できる。詳細は [§推奨](#recommend)・[§マトリクス](#matrix)・[§解決案](#resolve) へ。

**問1（`<Prjルート>\.claude` を Marketplace の仕組みで配布できるか）→ No（部分的にのみ可）。** Marketplace で配れるのは `.claude/` の一部＝**機能・拡張資産**（skills/commands/hooks/MCP/output-styles/agents 等）と plugin 専用コンポーネントに限られ、**`.claude/` 全体を単一 Marketplace では配れない**（`CLAUDE.md`/`rules`/`permissions` は plugin で運べない＝[§マトリクス](#matrix) ②）。

**問2（最小・ポータブルな `.claude/` を「Marketplace のように」共有する単一ベストプラクティスは存在するか／無ければ設計せよ）→ 単一の既製ベストプラクティスは存在しない。** 単一チャネルで全資産を運ぶ手段が無いため（[§配布チャネル](#channels)）、資産種別ごとに最適チャネルへ振り分けて設計する：

- **① 機能・拡張資産 → 層2（Plugin / Marketplace）** に集約（一元更新・版管理）
- **② ガバナンス資産（CLAUDE.md / rules / permissions） → 層1（Git commit）**。メンバーに**強制**したい組織は層3（Managed settings）を重ねる
- **③ 層2 を起動する設定（extraKnownMarketplaces / enabledPlugins） → 層1（project commit）**

**ポータブル最小 `.claude/` のスターター（層1 commit から始める出発点）**:

```txt
<repo>/
├── CLAUDE.md                     # チーム共通指示（層2 で運べない＝層1 commit）
└── .claude/
    ├── settings.json             # permissions（最小）＋ 層2 起動装置
    │                             #   （extraKnownMarketplaces + enabledPlugins）
    └── rules/*.md                # 常時ロードのトピック規約（必要なら）
# 機能資産（skills/commands/hooks/MCP）は層2 plugin から配り、repo には起動装置のみ残す
```

Git テンプレートリポジトリに置き "Use this template" で展開すれば、clone 直後（trust 承認後）に動く。**強制**が要る組織は層3 を重ねる。
**※ ゼロから即動く最小性を最優先するなら**、機能資産も `.claude/skills/` 等に同梱する従来型（層1 のみ）が最もポータブル（install/trust 不要。トレードオフは [§推奨](#recommend)）。

---

## 用語定義

本ドキュメントで一般語と異なる／特定の意味で用いる語を定義する。

| 用語 | 本ドキュメントでの意味 |
|---|---|
| **資産（asset）** | `.claude/` 配下に置く各構成要素（`CLAUDE.md` / `rules` / `settings.json` / `skills` / `agents` / `hooks` / MCP 定義 等）の総称。「配布対象となるファイル・設定」を指す。 |
| **配布チャネル（distribution channel）** | 資産をチームメンバーの環境へ届ける経路。本書では **層1（Git commit）／層2（Plugin・Marketplace）／層3（Managed settings）** の 3 つ。 |
| **強制力** | **配布した資産・ポリシーを、メンバーが上書き・無効化・不採用にできない度合い**（「配った資産を確実に使わせる」配布面の強制力に加え、層3 は permission/挙動ポリシーの強制＝`disableBypassPermissionsMode` 等による危険操作の禁止も含む）。3 段階＝**なし ＜ 弱 ＜ 最高**：層1 = なし（各自が自由に変更可）／層2 = 弱（`enabledPlugins` で有効化を誘導できるが install/無効化はメンバー可能）／層3 = 最高（managed は上書き不可）。 |
| **統制（governance / control）** | 組織が資産の内容・許可範囲を管理し、メンバー側での逸脱（勝手な権限緩和・無効化）を防ぐこと。主に層3 が担う。 |
| **一元更新** | 配布元を 1 か所更新すれば全メンバーへ反映できる性質（バージョン管理つき）。層2 の主利点だが、層3 の中央 push も同性質（版管理 UI の有無・強制の有無が層2 と異なる）。 |
| **ポータブル** | 特定リポジトリに依存せず、ゼロから作ったリポジトリでもそのまま動く最小構成であること。 |
| **スコープ** | 設定の適用範囲。global（`~/.claude`）／project（リポジトリ `.claude/`）／local（`settings.local.json`・個人用）／managed（組織配布）。 |
| **trust dialog（workspace trust）** | 信頼されていないフォルダを開いた際、project 由来の権限・skills 等を有効化する前に表示される承認ダイアログ。承認するまで commit 済み設定はフル有効化されない。 |
| **managed settings** | 組織管理者が OS レベル（MDM 等）または admin console 経由で配布する、ユーザーが上書きできない最上位設定（= 層3）。 |

---

<a id="prereq"></a>

## 前提情報：Claude Code の正規ファイルと資産の配置場所

Claude Code が読み込む正規ファイル・資産を**配置場所**で整理する。各々を層1/2/3 のどれで配布できるか（あるいは配布できないか）は次セクション以降で扱い、ここでは配置場所の把握のみを目的とする。出典は claude-directory のファイル一覧（[\[2\]](#ref-2)）と "What's not shown"（[\[3\]](#ref-3)）。

> **急ぐ読者は [§エグゼクティブサマリ](#summary) または [§推奨](#recommend) へ。本節は資産の所在を確認したい時の参照用。**

<a id="prereq-a"></a>

### (A) `~/.claude/` 配下に格納されるもの（個人・全プロジェクト共通）

home の `~/.claude/` 配下。個人の全プロジェクトに適用される（Windows は `%USERPROFILE%\.claude`。`CLAUDE_CONFIG_DIR` で別ディレクトリに変更可）。

| フォルダ | ファイル / 資産 | 説明 |
|---|---|---|
| `.` | `CLAUDE.md` | 全プロジェクト共通の個人指示 |
| `rules/` | `*.md` | 全プロジェクト共通のトピック別指示 |
| `.` | `settings.json` | 個人の既定 settings（permissions / hooks / env / モデル） |
| `skills/<name>/` | `SKILL.md` ＋任意のフォルダ・ファイル | 個人スコープの skill（補助ファイル同梱可。(B) と同形式。出典 [\[60\]](#ref-60)） |
| `commands/` | `*.md` | 個人スコープのコマンド（フラット単一ファイル） |
| `output-styles/` | `*.md` | 個人スコープの output-style |
| `agents/` | `*.md` | 個人スコープの subagent |
| `workflows/` | `*.js` | 個人スコープの動的ワークフロー |
| `agent-memory/<name>/` | （永続メモリファイル） | subagent の永続メモリ |
| `themes/` | `*.json` | 個人のカラーテーマ（global 専用） |
| `.` | `keybindings.json` | 個人のキーバインド（global 専用） |
| `projects/<project>/memory/` | （auto memory） | Claude のセッション横断自己メモ（global 専用） |
| `plugins/` | （install キャッシュ） | install 済み plugin・clone 済み marketplace のキャッシュ（`claude plugin` 管理。出典 [\[3\]](#ref-3)） |

<a id="prereq-b"></a>

### (B) 各プロジェクト直下の `.claude/` 配下に配置するもの

リポジトリの `.claude/` 配下（project スコープ）。

| フォルダ | ファイル / 資産 | 説明 |
|---|---|---|
| `.` | `CLAUDE.md` | プロジェクト指示（root の `./CLAUDE.md` でも可＝(C) 参照。出典 [\[52\]](#ref-52)） |
| `rules/` | `*.md` | トピック単位の指示（`paths:` でファイル種別にゲート可能） |
| `.` | `settings.json` | permissions / hooks / env / モデル既定値 |
| `.` | `settings.local.json` | 個人用オーバーライド（自動的に gitignore される） |
| `skills/<name>/` | `SKILL.md` ＋任意のフォルダ・ファイル | `/name` で呼ぶ／自動発火。補助ファイル（references / templates / examples / scripts 等）を同梱し `SKILL.md` から参照可（出典 [\[60\]](#ref-60)） |
| `commands/` | `*.md` | 単一ファイルのプロンプト。補助ファイル不可（新規は skills 推奨） |
| `output-styles/` | `*.md` | システムプロンプトの追加セクション（応答フォーマット定義） |
| `agents/` | `*.md` | 独自プロンプト・ツールを持つ subagent 定義 |
| `workflows/` | `*.js` | 複数 subagent を制御する動的ワークフロー（各ファイルが `/<name>`） |
| `agent-memory/<name>/` | （永続メモリファイル） | subagent の永続メモリ |

<a id="prereq-c"></a>

### (C) 各プロジェクト内のうち、`.claude/` 以外に配置する必要があるもの

project 内だが `.claude/` の外。ほとんどは**リポジトリ root 直下**だが、`CLAUDE.md` はサブフォルダにも置ける。

| フォルダ | ファイル / 資産 | 説明 |
|---|---|---|
| `.`（root）または `<dir>/` | `CLAUDE.md` | プロジェクト／ディレクトリ指示。`.claude/CLAUDE.md`（B）とも併用可。サブフォルダ版はそのディレクトリを参照した時にオンデマンドロード（出典 [\[52\]](#ref-52)） |
| `.`（root）または `<dir>/` | `CLAUDE.local.md` | `CLAUDE.md` と併読される個人用メモ。`.gitignore` 対象（出典 [\[53\]](#ref-53)） |
| `.`（root のみ） | `.mcp.json` | チーム共有する MCP サーバ定義 |
| `.`（root のみ） | `.worktreeinclude` | 新規 worktree にコピーする gitignore 済みファイルの指定 |

<a id="prereq-d"></a>

### (D) 上記以外の場所に格納されるもの

home（`~/.claude/`）でも project でもない場所。OS の管理者領域・システムパス・別リポジトリ等。

| ファイル / 資産 | 配置場所 | 説明 |
|---|---|---|
| `managed-settings.json` / `managed-settings.d/*.json` | macOS `/Library/Application Support/ClaudeCode/`、Linux・WSL `/etc/claude-code/`、Windows `C:\Program Files\ClaudeCode\`（出典 [\[36\]](#ref-36)） | 組織が配布する上書き不可の管理者設定 |
| plist / registry ポリシー | macOS `com.anthropic.claudecode` plist、Windows `HKLM\SOFTWARE\Policies\ClaudeCode` / `HKCU\...`（出典 [\[34\]](#ref-34)） | OS ポリシー経由の管理者設定 |
| `~/.claude.json` | home 直下（`~/.claude/` ではなく同階層の単独ファイル） | app state / OAuth / UI トグル / 個人用 MCP サーバ |
| plugin のソースリポジトリ | 別リポジトリ（install 後は (A) の `~/.claude/plugins/` にキャッシュ） | 下表の各コンポーネントを保持する。`.claude/` 配下には置けない |

plugin に梱包できる資産の一覧。**plugin 専用**（= (A)(B) の正規資産ではなく、plugin 経由でしか配布できない）か否かを併記する。作業指示者にも初見だった `bin` / `channels` / `monitors` を含む（出典 [\[4\]](#ref-4)）。

| コンポーネント | 既定の場所（plugin root 相対） | plugin 専用 | 説明 |
|---|---|:---:|---|
| `plugin.json`（manifest） | `.claude-plugin/plugin.json` | ✓ | plugin メタデータ。任意（standalone な `.claude/` 資産との区別は [\[9\]](#ref-9)） |
| skills | `skills/<name>/SKILL.md` | — | (A)(B) の正規資産でもある |
| commands | `commands/*.md` | — | (A)(B) の正規資産でもある |
| agents（subagents） | `agents/*.md` | — | (A)(B) の正規資産でもある |
| output-styles | `output-styles/*.md` | — | (A)(B) の正規資産でもある |
| themes | `themes/` | — | (A) global の正規資産でもある |
| hooks | `hooks/hooks.json` | — | hooks は settings.json でも定義可（(A)(B)） |
| MCP servers | `.mcp.json` | — | (C) の正規資産でもある |
| settings | `settings.json` | △ | plugin の既定 settings。`agent` / `subagentStatusLine` キーのみ対応（[\[6\]](#ref-6)） |
| **LSP servers** | `.lsp.json` | ✓ | 言語サーバ（Language Server Protocol）設定 |
| **Monitors** | `monitors/monitors.json` | ✓ | バックグラウンド監視の設定 |
| **Executables（bin）** | `bin/` | ✓ | plugin 有効時に Bash ツールの `PATH` に追加される実行ファイル群。bare command として実行可能 |
| **Channels** | `channels`（plugin.json 内のフィールド） | ✓ | 会話にコンテンツを注入。各チャネルは plugin 提供の MCP サーバにバインド（出典 [\[7\]](#ref-7)） |

> 凡例: **plugin 専用 ✓** = plugin 経由でのみ配布可（bin / channels / monitors / LSP / manifest）。**—** = (A)(B) 等の正規資産でもあり層1 でも配れる。**△** = plugin 文脈の専用ファイルだが対応キーが限定。

---

<a id="channels"></a>

## 資産を一元的に配布・統制する手段（配布チャネル）

前提情報 (A)〜(D) で挙げた資産を、チームへ**一元的に配布・統制する手段**を整理する。手段は **3 つの配布チャネル**に大別でき、各チャネルが運べる資産・更新性・強制力が異なる。**単一の手段で全資産を配ることはできず**、設計とは資産特性に応じてこの 3 チャネルを組み合わせることである。

運べる資産の詳細は次の [§マトリクス](#matrix)で網羅するため、ここでは概要に留める。

| チャネル | 運べる資産（概要） | 更新性 | 配布の強制力 | 仕組みづくりのコスト |
|---|---|---|---|---|
| **層1: Git テンプレート / commit** | **最も広い**（commit 可能な全資産） | 手動（各自 pull） | **なし**（各自が上書き可） | **低**：Git リポジトリに `.claude/` を commit するだけ（テンプレ化は任意） |
| **層2: Plugin / Marketplace** | 機能・拡張資産＋plugin 専用（LSP/bin 等） | **一元更新（バージョン管理）** | 弱（enabledPlugins で誘導。強制は層3併用） | **中**：plugin 化＋marketplace リポジトリ＋settings 設定（軽量化は §解決案 B'〜B'''） |
| **層3: Managed settings** | ガバナンス・制御資産＋skills/subagents | 中央 push | **最高（上書き不可）** | **高**（既存の IT 統制基盤があれば低）：MDM 等の管理者基盤が前提（下記補足） |

> **層3（managed）の仕組みづくりのイメージ**: 各マシンの **OS 管理者領域**へ設定を配るための IT 統制基盤が要る。具体的には (a) **MDM**（Jamf / Kandji / Intune / GPO 等）で `managed-settings.json` を OS パスへ配布、(b) **plist（macOS）/ レジストリ（Windows `HKLM`）** へポリシー配布、(c) **Claude for Teams / Enterprise** なら **Claude.ai admin console** から中央 push（server-managed・出典 [\[49\]](#ref-49)）。いずれも**管理者権限が前提で個人では構築できない**（公式に MDM スターターテンプレートあり。出典 [\[34\]](#ref-34) [\[35\]](#ref-35)。詳細は §解決案 案D）。

### 重要な点

- managed settings（層3）は統制（制限）の手段であると同時に、`CLAUDE.md`・permission rules・MCP・subagents・skills を**中央配布できる配布チャネル**でもある（出典 [\[27\]](#ref-27) [\[28\]](#ref-28) [\[39\]](#ref-39)）。
- `.claude/` のガバナンス資産（`CLAUDE.md` / `rules` / `permissions`）を、**メンバーが上書きできない形で（＝強制）**配布できる唯一の経路である。

### 注意事項

**「`.claude` が無いと claude が起動できない」は誤り。** "The rest of the directory is optional"（出典 [\[1\]](#ref-1)）。`.claude/` は起動の必須要件ではない。
Marketplace で `.claude/` 全体を配れないのは「起動必須だから」ではなく、**Marketplace の配布対象が plugin 単位に限定されているため**である。

---

<a id="matrix"></a>

## 核心：資産種別 × 配布チャネル マトリクス

設計判断の中核。前提情報の資産を**配布のふるまいが共通するグループ**に分け、各グループがどのチャネルで配布できるかを示す。資産が増えても視認性を保つため、単一の大表ではなくグループ別の小表に分割した。

> **凡例**: ✅ 配布可（全構成要素）／ ❌ 不可（機構が無く配布できない）／ △ **部分的・条件付き**（構成要素や設定の一部しか配布／適用されない）／ ? **公式記載なし・未確認** ／ — 非該当（そのチャネルの概念自体が資産に適用されない。例: `--add-dir` と MCP）。
> **チャネル列**: 層1（Git commit）・層2（Plugin）・層3（Managed）・`--add-dir`（補助参照）。
> **パス列**: 各資産の主な配置（commit できる資産は `.claude/` 等の層1 の場所、plugin 専用は plugin root）。資産の「チャネル」は**配置場所と対応**し（skill を `.claude/skills/` に置けば層1／plugin に同梱すれば層2／managed dir に置けば層3）、層2・層3 で場所が異なる資産はセルに併記する。
> **scope の差（重要）**: パス列の `.claude/` は **project スコープ**（`<project>/.claude/` ＝ 層1 commit 対象）を指す。同じ資産を **`~/.claude/`（ユーザーホーム ＝ global）** に置いた場合は**各自の個人設定**となり、team 配布チャネルにはならない（`~/.claude/` へ team が配る経路は無い）。よって `.claude` を含むパスの資産は「**project `.claude/` ＝ 層1 ／ `~/.claude/` ＝ 個人（非配布）**」という差がある（前提情報 (A) 参照）。

**① 機能・拡張資産 — 層1・層2 を主とし層3 でも配布可（一部に条件付き △/? あり）**

| 資産 | パス | 層1 commit | 層2 plugin | 層3 managed | `--add-dir` |
|---|---|:---:|:---:|:---:|:---:|
| `skills/` | `.claude/skills/<name>/` | ✅ | ✅ | ✅ Enterprise スコープ [\[38\]](#ref-38) | ✅ live reload（`--add-dir` のみ。`additionalDirectories` は❌）[\[25\]](#ref-25) |
| `agents/`（subagents） | `.claude/agents/*.md` | ✅ | △ `hooks`/`mcpServers`/`permissionMode` は無視（**これらを使う subagent は実質①の条件分岐＝層1/層3 が配布先**。[§推奨](#recommend) 参照）[\[39\]](#ref-39) | ✅ managed dir（最高優先）[\[39\]](#ref-39) | ❌ |
| `commands/` | `.claude/commands/*.md` | ✅ | ✅ 新規は `skills/` 推奨 | △ managed は skill 推奨 | ❌ |
| hooks | 設定＋スクリプト本体（配置はチャネル別。各セル参照） | ✅ `settings.json` ＋ repo 内スクリプト（`$CLAUDE_PROJECT_DIR` 参照）[\[61\]](#ref-61) | ✅ plugin に `hooks/hooks.json` ＋ `scripts/`（`${CLAUDE_PLUGIN_ROOT}`）を同梱し**自己完結** [\[4\]](#ref-4) | △ `allowManagedHooksOnly`。**設定のみ配布・スクリプト本体は別途配置** [\[28\]](#ref-28) | ❌ |
| MCP | `.mcp.json`（project root） | ✅ | ✅ [\[4\]](#ref-4) | ✅ `allowedMcpServers` ＋ `allowManagedMcpServersOnly` [\[28\]](#ref-28) | — |
| `output-styles/` | `.claude/output-styles/*.md` | ✅ | ✅ [\[42\]](#ref-42) | ? 公式記載なし（→[§確認できない点](#unknowns)） | ❌ |

**② ガバナンス・制御資産 — 層2（plugin）では運べない**（層1 commit か 層3 managed のみ。配布設計の最重要制約）

| 資産 | パス | 層1 commit | 層2 plugin | 層3 managed | `--add-dir` |
|---|---|:---:|:---:|:---:|:---:|
| `CLAUDE.md` | `./CLAUDE.md` or `.claude/CLAUDE.md` | ✅ | ❌ **plugin root の CLAUDE.md は非ロード**（代替=skill 化）[\[5\]](#ref-5) | ✅ `claudeMd` / OS-path [\[27\]](#ref-27) | △ 環境変数で可 [\[26\]](#ref-26) |
| `rules/` | `.claude/rules/*.md` | ✅ | ❌ 運ぶ仕組みの記載なし | △ managed `CLAUDE.md` 経由のみ（単体は明記なし） | △ 同環境変数 [\[26\]](#ref-26) |
| `settings.json` の `permissions` | `.claude/settings.json` | ✅ 強制力なし | ❌ plugin settings は `agent`/`subagentStatusLine` のみ [\[6\]](#ref-6) | ✅ managed rules ＋ `allowManagedPermissionRulesOnly` [\[28\]](#ref-28) | ❌ |
| `enabledPlugins` / `extraKnownMarketplaces`（※層2 起動装置） | `.claude/settings.json` | ✅ [\[12\]](#ref-12) | ❌ plugin 単位では運べない | ✅ force-enable / 全スコープ install ブロック [\[40\]](#ref-40) | ✅ 読まれる [\[41\]](#ref-41) |

**③ plugin 専用コンポーネント等 — 層2（plugin）のみ**（配置場所は [§前提情報 (D)](#prereq-d)。本表は**配布チャネル**の可否に絞る）

| 資産 | パス | 層1 commit | 層2 plugin | 層3 managed | `--add-dir` |
|---|---|:---:|:---:|:---:|:---:|
| monitors / LSP / bin / channels | plugin root（`monitors/`・`.lsp.json`・`bin/`・`channels`） | ❌ `.claude/` 直下は非標準 | ✅ [\[4\]](#ref-4)[\[7\]](#ref-7) | — | ❌ |
| `themes/` | `~/.claude/themes/`（global）/ plugin `themes/` | ❌ project commit 不可 [\[43\]](#ref-43) | ✅ experimental [\[43\]](#ref-43) | — | ❌ |

**④ 層1（commit）でのみ配布できる資産**

| 資産 | パス | 層1 commit | 層2 plugin | 層3 managed | `--add-dir` |
|---|---|:---:|:---:|:---:|:---:|
| `workflows/*.js` | `.claude/workflows/*.js` | ✅ [\[44\]](#ref-44) | ❌ plugin 記載なし | — | ❌ |
| `agent-memory/`（project） | `.claude/agent-memory/<name>/` | ✅ VCS でチーム共有可 [\[45\]](#ref-45) | ❌（運ぶ仕組みなし） | ❌（運ぶ仕組みなし） | — |

**配布対象外（どのチャネルでも一元配布できない）** — 個人用・端末ローカルのため一元配布不可（前提情報 (D) 参照）:

- `settings.local.json`（プロジェクト個人設定。自動 gitignore）
- `CLAUDE.local.md`（個人メモ。`.gitignore` 対象）
- `~/.claude.json`（app state / OAuth / 個人用 MCP）
- auto memory（`~/.claude/projects/<project>/memory/`）
- `keybindings.json`（個人キーバインド）

> 組織共通にしたい場合は、層3（managed）で**同種の内容**を別途配布する。

---

<a id="resolve"></a>

## 解決案（チャネル別 + トレードオフ）

[§前提情報](#prereq) が資産の**配置場所**を、[§マトリクス](#matrix) が**資産 × チャネルの配布可否**を示したのに対し、本節は**チャネル軸**で各チャネルの実装案と、[§配布チャネル](#channels) で示した**コスト・強制力・更新性のトレードオフ**を扱う。資産ごとの配布可否・配置場所は前掲 2 節に譲り、本節では**チャネル固有の実装と注意点**に絞る（資産一覧は再掲しない）。

本節は各チャネルで「何を・どう配れるか」を並べる**実装カタログ**であり、チャネルの優劣を論じるものではない。**どの資産をどのチャネルへ寄せるか（組み合わせ方針）は次の [§推奨](#recommend) で扱う**——本節＝チャネル別の実装手段、§推奨＝資産の振り分け方針、という役割分担である。

提示順は **層1 → 層2 → 補助（`--add-dir`）→ 層3** とし、[§配布チャネル](#channels) の **コスト 低→高／強制力 なし→最高** の並びに対応する（順序はコスト軸の説明都合であり、層1 を基盤・主役とする趣旨ではない）。各小節の冒頭にそのチャネルのコスト／強制力／更新性を再掲する。`--add-dir` は主配布チャネルではないため補助系として層2 と層3 の間に置く。層2 は 4 案を横並び比較できるよう 1 表にまとめる。

### 層1系（Git テンプレート / commit）

> **コスト 低｜強制力 なし（各自が上書き可）｜更新性 手動（各自 pull）**

| 案 | 概要 | 主な長所 | 主な短所・制約 | 使いどころ |
|---|---|---|---|---|
| **案A** | Git リポジトリ（テンプレート／既存）に `.claude/` を commit。"Use this template" 等で展開 | ゼロから作るリポジトリでもそのまま動く。**IT 統制基盤なしに（層3 を持たない組織でも）層2 で運べないガバナンス資産（CLAUDE.md/rules/permissions）と層2 起動装置を配れる唯一の経路**。commit 可能な資産は技術的には最も広く運べる | 更新伝播が手動（各自 pull）。**配布の強制力なし**。trust 承認後に有効化。project で無視される security キーあり。配列設定は結合マージ | **層2 で運べない資産（ガバナンス／層1専用）＋層2 起動装置**の配布。機能資産も技術的には載るが、更新性のため層2 へ寄せるのが推奨（→[§推奨](#recommend)） |

**補足（案A）**:

**［利点・活用］**

- **commit 対象（層1 が"運べる"資産の全体）**: `CLAUDE.md`/`rules/*.md`/`settings.json`/`skills/`/`agents/`/`commands/`/`output-styles/`/`workflows/*.js`/`.mcp.json`/`.worktreeinclude`/`agent-memory/`（[前提情報 (B)](#prereq-b)（`.claude/` 配下）＋(C)（root の `.mcp.json`/`.worktreeinclude`）・出典 [\[2\]](#ref-2)）。[§マトリクス](#matrix) で層1 が ✅ の資産すべてに対応する。**これは層1 で配布"できる"資産の全体であって"載せるべき"資産ではない**——機能資産は層2 へ寄せ、層1 には層2 で運べないものを残すのが推奨（振り分けは [§推奨](#recommend)）。
- **hooks の配布**: hook 設定（`settings.json`）と repo 内のスクリプト本体（`$CLAUDE_PROJECT_DIR` 相対。例 `scripts/`・`.claude/hooks/`）の**両方を commit** する（出典 [\[61\]](#ref-61)）。
- **rules の軽量共有**: `.claude/rules/` はシンボリックリンク可（`ln -s ~/shared-rules .claude/rules/shared`。出典 [\[24\]](#ref-24)）。
- **💡 `--settings` 起動による強化（応用）**: ランチャースクリプト（層1 で commit）から `claude --settings <path>` で起動すると、`<path>` の settings は **command-line precedence（managed 直下・local/project/user より上）** で適用される（出典 [\[62\]](#ref-62)）。層3 が無くても通常 settings（`permissions`/`hooks`/`env` 等）を高優先で効かせられる。**ただし公式は `--settings` を automation/CI・background(agent view)・Agent SDK 向けの per-invocation / セッション限りの override として説明しており**（出典 [\[66\]](#ref-66)）、対話運用での恒久的な設定共有手段とは位置づけていない（正規ルートは project commit／層3）。本テクはその転用で、**強制力は弱**（ランチャー経由起動を強制できず、値ファイルも書換可）。`defaultMode: auto` 等の security 系キーは project/local で無視される仕様だが、**`--settings` 経由の値がどのスコープ判定を受けるか**（command-line として特別扱いされ有効化されるのか、依然 project 相当で無視されるのか）が原文に不記載で、効果は不確定（推測領域）。

**［注意事項 ⚠］**

- **project settings で無視される security キー**（commit しても効かない）: `defaultMode: auto`（v2.1.142+）／`skipDangerousModePermissionPrompt`／`autoMode`・`useAutoModeDuringPlan`（出典 [\[21\]](#ref-21)）。「リポジトリが自分に auto mode を付与する」攻撃を防ぐため、project/local settings からは読まれない。
- **trust dialog 条件**: commit した `.claude/skills/` の `allowed-tools` や project settings の permission は、**workspace trust 承認後**に有効化（出典 [\[22\]](#ref-22)）。clone 即フル機能ではない。
- **配列設定のマージ**: `permissions.allow` 等の配列はスコープ間で**結合＋重複除去**（置換ではない。出典 [\[23\]](#ref-23)）。user 設定と結合され、予期せぬ許可が生じうる。

**［制約］**

- **settings の外部化に「ファイル内 import」は無い**: `settings.json` から別パスの settings を取り込む `extends`/`import` 相当のキーは存在しない。外部化手段は階層マージ（managed>local>project>user）／`--settings`／`managed-settings.d/`（層3 のみ）。`CLAUDE.md` には `@path` import 構文があるが **memory 専用**で settings には適用されない（出典 [\[67\]](#ref-67)）。

**層1 の配布パターン（運用形態）**:

層1（commit／clone ベース）の資産を実際にチームへ届ける運用は、3 パターンに整理できる。**層2 で配れる機能資産は層2（Plugin/Marketplace）へ寄せ、本パターンは「層2 で配れない資産」を対象とする**（振り分けは [§推奨](#recommend)）。起動オプションの全容は [付録B](#appendix-b) を参照。

- **パターン1（作業リポジトリに commit）**: 作業する Git リポジトリそのものに `.claude/` 資産を commit してチーム共有。**各リポジトリの特性に合わせた固有資産**（そのプロジェクト専用の `CLAUDE.md`/`rules`/`settings.json` 等）が対象。
- **パターン2（共通リポジトリ `<D>` を clone ＋ 起動オプションで参照）**: Claude Code 資産だけを一元管理する共通リポジトリ `<D>` を各自が clone し、**起動ランチャーで起動オプションを付与して読み込ませる**。作業リポジトリに依らない**横断的・チーム共通**の資産のうち、**層2 で配れないもの**が対象。`<D>` を clone しただけでは Claude は読まないため、**「clone ＋ 起動オプション」の二点セット**で初めて有効になる:
  - CLAUDE.md / rules → `--add-dir <D>` ＋ 環境変数 `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1`（[§マトリクス](#matrix) ②・出典 [\[26\]](#ref-26)）
  - permissions → `--settings <D>/.claude/settings.json`（command-line precedence で project 等とマージ・出典 [\[62\]](#ref-62)）
  - MCP（`.mcp.json`）→ `--mcp-config <D>/.mcp.json`（MCP は層2 でも配れるため層1 で配るかはチーム判断）
- **パターン3（別リポジトリから手動で作業リポジトリ直下へ移動）**: パターン2 の `<D>` とは別リポジトリで管理し、各自が clone 先から**手動でファイルを作業リポジトリ直下へ移動**して使う。作業リポジトリに依らない資産だが、**層2 で配れず、かつ起動オプションでも渡せず、作業 Git リポジトリ直下への物理配置が必須**なものが対象。現状は **起動ランチャースクリプト**（パターン2 の起動オプションを付与する起動装置そのもの。作業フォルダ直下で実行する必要があり、自己参照的にこれだけは手動配布が避けられない）が該当する。
  - なお [(C)](#prereq-c) の `.worktreeinclude` も「リポジトリ直下必須・フラグで渡せない」点でパターン3 の形式に当たるが、**worktree 運用（明示 `--worktree`／background agents・agent view の自動 worktree／`isolation: worktree` 指定の subagent）をするチームの場合のみ必要**。foreground の subagent orchestration では worktree は生成されないため、**worktree を使わない方針なら配布対象外**（worktree 自体の概要は本書スコープ外）。

### 層2系（Plugin / Marketplace）

> **コスト 中｜強制力 弱（`enabledPlugins` で誘導。強制は層3 併用）｜更新性 一元更新（バージョン管理）**

| 案 | 概要 | 主な長所 | 主な短所・制約 | 使いどころ |
|---|---|---|---|---|
| **案B** | 共有資産を plugin 化し private Marketplace で配布。`settings.json` に `extraKnownMarketplaces`+`enabledPlugins` を commit | バージョン管理・一元更新。名前空間で衝突回避。`--scope project` でチーム共有 [\[10\]](#ref-10) | `CLAUDE.md`/`rules`/`permissions` は運べない（[§マトリクス](#matrix) ②）。コマンドが `/plugin:name` 化 [\[11\]](#ref-11)。enabledPlugins は trust 時 install プロンプト＋fresh machine で要 install [\[13\]](#ref-13)[\[14\]](#ref-14) | plugin が**多数・高頻度更新・複数リポ横断** |
| **案B'（インライン宣言型）** | `extraKnownMarketplaces` に `source:'settings'` で plugin を直接インライン宣言 [\[15\]](#ref-15) | 層1↔層2 を 1 ファイルに統合。**専用 Marketplace リポジトリ不要** | plugins は外部ソース（GitHub/npm 等）参照必須。enable は `enabledPlugins` 別途 | plugin が**少数・低更新** |
| **案B''（@skills-dir 型）** | `.claude/skills/<name>/` に `.claude-plugin/plugin.json` を置き `<name>@skills-dir` として自動ロード [\[16\]](#ref-16) | **marketplace/install 不要**。次セッションから自動ロード。`claude plugin init` で scaffold | project スコープは **monitors 無効**・MCP は per-server 承認・**trust 必須**・サブディレクトリ起動時は非検出。`plugin init` は個人スコープのみ scaffold [\[16\]](#ref-16) | marketplace を立てずに**最小の plugin 配布** |
| **案B'''（seed 焼き込み型）** | `CLAUDE_CODE_PLUGIN_SEED_DIR` でビルド時に plugins を焼き込み、起動時に runtime クローンなしで利用 [\[17\]](#ref-17) | runtime クローン依存・ネットワーク前提を解消。fresh machine の install 落とし穴を回避。read-only・seed 優先 | ビルド/CI への組み込みが前提 | **コンテナ/CI 環境**への焼き込み |

**補足（層2 共通）**（案B'〜B''' 固有の注意は上の比較表を参照）:

**［利点・活用］**

- **plugin が梱包できる資産の全容**は[前提情報 (D)](#prereq-d)・[§マトリクス](#matrix) ③ を参照（skills〜MCP の機能資産 ＋ LSP/monitors/bin/channels の plugin 専用コンポーネント。出典 [\[4\]](#ref-4)）。
- **plugin の配置メカニズム**: 有効化された plugin はキャッシュへ独立コピーされ、外部ディレクトリは参照しない（symlink は例外。出典 [\[8\]](#ref-8)）。
- **hooks は層2 で自己完結**: plugin に `hooks/hooks.json`（設定）とスクリプト本体を同梱し `${CLAUDE_PLUGIN_ROOT}` で参照すれば、設定もスクリプトも plugin 内で完結して配れる（層1 のような repo 側スクリプト配置に依存しない。出典 [\[4\]](#ref-4) [\[61\]](#ref-61)）。
- **新オプション**: `defaultEnabled: false`（インストールするが初期無効＝段階的ロールアウト）／`strict: false`（marketplace エントリが plugin.json より権威を持ち plugin 内容を完全制御）／release channel（同一 repo の異なる ref を 2 marketplace 化し managed settings でグループ別割当＝stable/latest）（出典 [\[18\]](#ref-18)）。
- **private repo 認証**: 自動更新には `GITHUB_TOKEN`/`GH_TOKEN`・`GITLAB_TOKEN`/`GL_TOKEN`・`BITBUCKET_TOKEN`（出典 [\[19\]](#ref-19)）。手動 install は credential helper で可（トークン不要）。
- **monorepo 配布**: `git-subdir` ソース / `--sparse` で必要ディレクトリのみ取得。npm private registry ソースも可（出典 [\[20\]](#ref-20)）。
- **`autoUpdate` が「一元更新」の実体**: marketplace 内の plugin 更新は各 `extraKnownMarketplaces` エントリの `autoUpdate`（起動時 refresh）が担う。**公式 Anthropic marketplace は default `true`／それ以外は default `false`** なので、自動更新したい組織 marketplace は明示 `"autoUpdate": true` を付けるか managed で強制する。ソース定義自体は marketplace の増減時のみ変更で低頻度（出典 [\[65\]](#ref-65)）。

**［注意事項 ⚠］**

- **△ subagent の一部設定は無効化**: plugin 経由の subagent は `hooks`/`mcpServers`/`permissionMode` が無視される（[§マトリクス](#matrix) ① の △）。これらを効かせたい subagent は層1 commit か層3 managed で配る（出典 [\[39\]](#ref-39)）。
- **enabledPlugins は「自動有効化」ではない**: `extraKnownMarketplaces`+`enabledPlugins` を commit しても、trust 時に**インストールを促すプロンプト**が出る（出典 [\[13\]](#ref-13)）。さらに **fresh machine では cache されておらず `claude plugin install` が必要**（出典 [\[14\]](#ref-14)）。「clone だけで全 plugin 即利用可」ではない。
- **起動装置（`enabledPlugins`/`extraKnownMarketplaces`）のスコープと頻度**: 両者は `settings.json` の設定値。`enabledPlugins` は `"plugin@marketplace": true/false` の boolean マップで、一度有効化すれば plugin を増減しない限り変更不要（削除時は stale entry の掃除が要る）。スコープは **user(`~/.claude/settings.json`)＝個人の全プロジェクト共通／project(`.claude/settings.json`)＝チーム配布／local／managed**、precedence は managed>local>project>user（出典 [\[63\]](#ref-63)）。**user スコープはチーム配布にならない**——`~/.claude/` は repo 外で他者に渡らず web セッションにも乗らないため、全員へ効かせるには project に commit する（出典 [\[64\]](#ref-64)）。

**［制約］**

- **`rules` の代替（条件付きのみ層2 可）**: `rules` は plugin で直接運べないが、**条件付き rule（`paths:` で特定ファイル種別に限定）は skill として実装すれば層2 で配れる**——skill も `paths:` frontmatter を持ち（path-specific rules と同 format）、`user-invocable: false` で `/` 補完にも出さない（出典 [\[68\]](#ref-68)）。**ただし「常時ロードしたい rule」は skill 化では代替できない**: skill は「呼び出し時／関連と判断した時／`paths` 該当時」のみロードで常時ロードが無く、公式も「常時は rules/CLAUDE.md、必要時は skills」と用途を分けている（出典 [\[69\]](#ref-69)）。`CLAUDE.md` の `@import` で SKILL.md を常時展開する迂回も、import は内容をテキスト展開するだけで skill 機構（frontmatter・progressive disclosure 等）が死に、glob 非対応（単一パス・再帰4ホップ）・plugin のキャッシュパスも不安定なため非現実的（出典 [\[70\]](#ref-70)）。常時 rule は層1（commit/`--add-dir`）か層3（managed）に残す。

**補足（案B''')**: 複数 seed は Unix `:` / Windows `;` で多層化。ビルド時直接 install は `CLAUDE_CODE_PLUGIN_CACHE_DIR`（出典 [\[17\]](#ref-17)）。

### 補助系（`--add-dir`）

> **主配布チャネルではない補助参照｜強制力 なし｜実体（追加ディレクトリ）の配布は層1 等が前提**

`--add-dir` は 3 つの主配布チャネル（層1/2/3）には数えない**補助参照機構**。層番号は付さない。**配布チャネルに数えない判定基準**＝ (1) 資産の実体を運ばず（参照のみ）、(2) 起動フラグ/環境変数の手動付与に依存し恒久設定として宣言・commit できない——この 2 点で層1（実体を repo で運び commit 宣言できる）と峻別される。実体配布は層1 等に依存する。

| 案 | 概要 | 主な長所 | 主な短所・制約 | 使いどころ |
|---|---|---|---|---|
| **案C** | `claude --add-dir ../shared-config` で共有リポジトリの skills を参照。`CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` で `CLAUDE.md`/`rules` も読む [\[26\]](#ref-26) | 複数リポジトリで共通の skills 等を 1 箇所に括り出して参照（DRY）。参照なので 1 箇所更新が反映される | **追加ディレクトリ自体は各自が clone 等で別途取得する必要がある**（`--add-dir` は配布手段ではなく参照のみ。実体の配布は層1 等が前提）。agents/commands/hooks/output-styles/permissions は不可。`permissions.additionalDirectories` 設定では **skills すら読まれない**（`--add-dir` フラグ専用例外）[\[25\]](#ref-25)。起動フラグ運用が煩雑 | **補助的**。共有の主役にはならない |

**補足（案C）**:

**［利点・活用］**

- **チーム共通 CLAUDE.md/rules の配布**: `--add-dir <D>` ＋ `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` を設定すると、`<D>/CLAUDE.md`・`<D>/.claude/CLAUDE.md`・`<D>/.claude/rules/*.md`・`<D>/CLAUDE.local.md` がロードされる（`<D>/CLAUDE.md` と `<D>/.claude/CLAUDE.md` は**両方**読まれ等価。環境変数を付けないと既定では読まれない。出典 [\[71\]](#ref-71)）。層2 で運べない**常時ガバナンス（CLAUDE.md/rules）を共通フォルダ 1 箇所で参照させる**用途に使える（実体は各自 clone・強制力なしは案C 共通）。

**［注意事項 ⚠］**

- **探索仕様（フォルダ名の落とし穴）**: `--add-dir <D>` は `<D>` を作業ルートに加え、設定を **`<D>/.claude/` 配下**から読む（skills は `<D>/.claude/skills/`。出典 [\[25\]](#ref-25)）。したがって共有設定リポジトリを clone する際に**フォルダ名自体を `.claude` にすると `<D>/.claude/.claude/` を探すことになり中身が読まれない**。`--add-dir` に渡すのは「`.claude/` を内包する親フォルダ」にする（例: `~/shared/team-config` を渡し `~/shared/team-config/.claude/skills/` を読ませる）。
- **競合**: `<D>/.claude/` とプロジェクトの `.claude/` は別パスのため FS 競合は起きない（Claude は両者を区別）。ただし資産は結合され（skills 両ロード、`enabledPlugins`/`extraKnownMarketplaces` はマージ）、同名資産があれば名前レベルの衝突はありうる（フォルダ名とは無関係）。

### 層3系（Managed settings）— 配布 ＋ 強制

> **コスト 高（MDM 等の IT 統制基盤が前提）｜強制力 最高（上書き不可）｜更新性 中央 push**

| 案 | 概要 | 主な長所 | 主な短所・制約 | 使いどころ |
|---|---|---|---|---|
| **案D** | managed settings で permission/MCP/`CLAUDE.md`/subagents/skills/version を管理者制御ティアで配布。**上書き不可** [\[37\]](#ref-37) | **唯一の強制手段**。`CLAUDE.md`/permissions の中央配布も担う | IT 統制（MDM / admin console）が前提。server-managed は **Claude for Teams / Enterprise** 機能 [\[49\]](#ref-49)。マシン/組織単位 | 「推奨」でなく「**強制**」したい・plugin 以外の customization を禁止したい組織 |

**補足（案D）詳細**:

**［注意事項 ⚠］**

- **商用プラン要件**: **server-managed settings（Claude.ai admin console 経由）は Claude for Teams または Enterprise の機能**（Teams は v2.1.38+、Enterprise は v2.1.30+。device management 不要。出典 [\[49\]](#ref-49)）。一方 **file-based `managed-settings.json`（MDM/plist/registry 配置）は OS レベルの管理者機構**で、admin 権限があれば配置可能（＝商用プランが無くても MDM 経由の層3 統制は可能。なお file-based のプラン非依存はファイル機構の性質からの推定で原文に明示はない）。
- **server-managed のバイパス**: third-party provider 使用時（`CLAUDE_CODE_USE_BEDROCK`/`_VERTEX`/`_FOUNDRY` や非既定 `ANTHROPIC_BASE_URL`）は server-managed が**バイパスされる**（出典 [\[48\]](#ref-48)）。endpoint-managed の併用が必要。

**［利点・活用（配布・統制でできること）］**

- **配布メカニズムの 4 層と優先順位**（出典 [\[34\]](#ref-34)）:
  1. Server-managed（Claude.ai admin console、最高、全OS）
  2. plist / registry（macOS `com.anthropic.claudecode` plist ／ Windows `HKLM\SOFTWARE\Policies\ClaudeCode`、高）
  3. File-based（`managed-settings.json` ＋ `managed-settings.d/*.json`、中）
  4. Windows user registry（`HKCU\...`、最低）
  - **managed tier 間はマージしない（先勝ち）**。server-managed が非空なら endpoint-managed は完全無視。file-based tier 内のみ base + `managed-settings.d/` がマージ。
- **OS別 file-based パス**: macOS `/Library/Application Support/ClaudeCode/`／Linux・WSL `/etc/claude-code/`／**Windows `C:\Program Files\ClaudeCode\managed-settings.json`**（旧 `C:\ProgramData\...` は **v2.1.75 以降廃止**。出典 [\[36\]](#ref-36)）。
- **`managed-settings.d/`**: 複数チームの独立ポリシー断片をアルファベット順マージ（数値プレフィックスで順序制御。スカラー後勝ち・配列結合＋重複除去・オブジェクト deep merge）。MDM starter templates（Jamf/Kandji/Intune/GPO）が公式 repo に（出典 [\[35\]](#ref-35)）。
- **層3で配布できる資産（配布チャネルとしての側面）**:
  - **組織共通 CLAUDE.md**: OS-path に `CLAUDE.md` 配置 or `managed-settings.json` の `claudeMd` キー埋め込み（全セッション強制ロード・除外不可。出典 [\[27\]](#ref-27)）。
  - **permission rules**: managed に置き `allowManagedPermissionRulesOnly` で user/project の permission 定義を排除（出典 [\[28\]](#ref-28)）。
  - **MCP**: `allowedMcpServers` ＋ `allowManagedMcpServersOnly`（出典 [\[28\]](#ref-28)）。`managed-mcp.json` は server-managed では配布不可・キーで代替（出典 [\[46\]](#ref-46)）。
  - **managed subagents / skills**: managed settings dir の `.claude/agents/`・skills（Enterprise スコープ）が最高優先（出典 [\[39\]](#ref-39) [\[38\]](#ref-38)）。
  - **enabledPlugins**: 全スコープの install をブロック ＋ marketplace から非表示する組織ポリシー（出典 [\[40\]](#ref-40)）。
  - **hooks（△ 部分配布）**: `allowManagedHooksOnly` で hook を managed 由来のみに制限できるが、**配布できるのは設定のみ・スクリプト本体は別途配置が必要**で層3 単独では完結しない（[§マトリクス](#matrix) ① の △。出典 [\[28\]](#ref-28) [\[61\]](#ref-61)）。
- **層3の統制（強制）キー一覧**（20+ キー、いずれも Managed settings only）: 視認性のため一覧は文末の [付録A：層3 統制キー一覧](#appendix-a) に分離した。代表例＝`strictPluginOnlyCustomization`（skills/agents/hooks/MCP を plugin+managed 由来のみに制限）／`allowManagedPermissionRulesOnly`（permission を managed のみに）／`requiredMinimumVersion`（範囲外バージョンで起動拒否）／`strictKnownMarketplaces`（marketplace 許可リスト）。
- **検証**: `/status` の `Setting sources` で `(remote)`/`(plist)`/`(HKLM)`/`(HKCU)`/`(file)` を確認（出典 [\[47\]](#ref-47)）。設定が反映されない場合は `/debug`（debug-your-config）で設定ソースのロード状況を診断（出典 [\[72\]](#ref-72)）。

**［制約・スコープ外］**

- **一覧から除外した managed 専用キー**（漏れではなくスコープ外として明示）: `sshHostAllowlist`（Claude **Desktop アプリ専用**で CLI/IDE は読まない）、`bwrapPath` / `socatPath`（Linux/WSL2 のサンドボックス binary パス）。いずれも managed 専用だが `.claude/` 資産の配布統制とは目的が異なる（出典 [\[59\]](#ref-59)）。

---

<a id="recommend"></a>

## 推奨：機能資産は層2 へ集約し、層1・層3・`--add-dir` で役割分担

**配布の原則**: 層1/層2 の両方で配れる機能資産（skills/commands/hooks/MCP/output-styles 等）は、**強制力ゼロ・更新手動の層1ではなく、一元更新できる層2（plugin）へ一律寄せる**。層1 は「層2 で運べない資産」と「層2 を起動するための設定」に絞り、二重管理をなくす。

```txt
■ 原則: 層1/層2 両対応の機能資産は層2 へ一律集約。層1 は「層2 で運べない資産」と「層2 の起動装置」に絞る。

┌─ 層2 ── 機能・拡張資産を集約（案B〜B'''／更新性 ◎・強制力 弱）
│   skills/ commands/ hooks/ MCP/ output-styles/ ＋ △以外の agents/
│   ＋ plugin 専用（マトリクス③）: LSP/ monitors/ bin/ channels/
│   ⚠ install/trust・fresh machine で要 install・ネットワーク前提（即動ポータビリティ ↓）
│
├─ 層1 ── 層2 で運べない資産 ＋ 層2 起動装置（案A／強制力 なし）
│   ガバナンス（マトリクス②）: CLAUDE.md / rules/ / permissions
│   起動装置: settings.json の extraKnownMarketplaces + enabledPlugins（無いと層2 が有効化されない）
│   層1専用（マトリクス④）: workflows/ / agent-memory/
│   △回避: hooks/mcpServers/permissionMode を使う subagent（plugin だと無視されるため層1 へ）
│
├─ 補助 ── --add-dir（案C／参照のみ・強制力 なし）
│   層2 で運べない CLAUDE.md / rules を共有ディレクトリから参照（skills の --add-dir live reload と同性格）
│
└─ 層3 ── ガバナンス資産を「強制」（案D／IT 統制がある組織・強制力 最高）
    CLAUDE.md(claudeMd) / permissions / MCP / subagents / skills / version / strict 系
    → 上書きを防げるのは層3 だけ。中央 push。Windows=C:\Program Files\ClaudeCode\・複数チーム=managed-settings.d/
```

**役割分担の早見（どの資産をどこへ寄せるか）**: 配布可否そのものは [§マトリクス](#matrix)、各チャネルの実装と注意点は [§解決案](#resolve) に詳述済み。本表は方針（"寄せる先"）の早見に絞り、根拠は両節への参照に留める。

| 資産グループ | 寄せる先 | 根拠（詳細はマトリクス／解決案） |
|---|---|---|
| 機能・拡張（skills/commands/hooks/MCP/output-styles） | **層2** | 一元更新・版管理が効く（マトリクス①） |
| subagents | 原則 **層2**／ `hooks`・`mcpServers`・`permissionMode` を使うものは **層1**（or 層3） | plugin 経由は当該設定が無視（①△） |
| ガバナンス（CLAUDE.md/rules/permissions） | **層1**（強制が要れば層3） | 層2 では運べない（②）。例外＝条件付き rule のみ skill 化で層2 可 |
| 層2 起動装置（extraKnownMarketplaces/enabledPlugins） | **層1**（project commit） | 層2 有効化の前提。user スコープはチーム配布にならない |
| 層1専用（workflows/agent-memory） | **層1** | 層2 に運ぶ仕組みがない（④） |
| plugin 専用（LSP/monitors/bin/channels） | **層2** | plugin 経由でのみ配れる（③） |

**強制できるのは層3 だけ**: 層1・`--add-dir` は強制力なし、層2 も弱（`enabledPlugins` で誘導するのみで install/無効化は各自可）。メンバーの上書きを防いで**強制**したい、または plugin 以外の customization を禁止（`strictPluginOnlyCustomization`）したい組織は層3（managed）を重ねる。層3 は制限機構であると同時に、ガバナンス資産（CLAUDE.md/permissions/MCP/subagents/skills）の**中央配布チャネル**でもある（詳細は [§解決案](#resolve) 層3系・案D）。

**`--add-dir` で補える近縁資産**: 層2 で運べない CLAUDE.md/rules は、`--add-dir` ＋ `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` で共有ディレクトリから参照する DRY 共有ができる（skills の `--add-dir` live reload と同じ "参照型共有" の性格）。ただし実体は各自 clone が前提・強制力なしで、補助の位置づけに留まる（詳細は [§解決案](#resolve) 補助系・案C）。

**トレードオフ（ポータビリティ）**: 機能資産を層2 へ寄せると install/trust/ネットワーク前提が加わり、「clone（commit）だけで即動く」最小ポータビリティ（問い2）は下がる。ゼロ立ち上げの即動性を最優先するなら、機能資産を層1 に同梱する従来型も依然有効。ネットワーク前提・fresh machine の `claude plugin install` を解消したい場合は [§解決案](#resolve) 案B'''（seed 焼き込み型・`CLAUDE_CODE_PLUGIN_SEED_DIR`）を併用する。

**結論の要約**（[§エグゼクティブサマリ](#summary) の詳細版）:

- **問1 への回答**: Marketplace で配れるのは `.claude/` の一部＝機能・拡張資産＋plugin 専用コンポーネントに限られ、**`.claude/` 全体を単一 Marketplace では配れない**（CLAUDE.md/rules/permissions は plugin 不可＝[§マトリクス](#matrix) ②）。
- **問2 への回答**: 「最小 `.claude/` を Marketplace のように共有する」**単一の既製ベストプラクティスは存在しない**（単一チャネルで全資産を運べないため）。代替として、資産を「**層2 で運べないもの（層1／強制が要れば層3）**」と「**両方で配れる機能資産（層2 へ集約）**」に振り分けて設計した。`--add-dir` は層2 で運べない CLAUDE.md/rules の DRY 共有を補助する。skills/subagents は公式も「Commit / Plugin / Managed の 3 経路」で整理している（出典 [\[50\]](#ref-50)）。

---

<a id="unknowns"></a>

## 確認できない点（推測領域）

ガバナンス設計上、公式ドキュメントに根拠が無く推測に留まる点を明示する。

- 3 チャネルを結合する scaffold/onboarding スクリプトの**公式手順は記載なし**（運用設計＝推測領域）。
- `rules/` を **plugin 単体**でそのまま運ぶ仕組みは原文に記載なし（plugin の正規コンポーネントに rules は無く、managed での rules 単体配布も明記なし）。回避策として **条件付き rule（`paths:` 付き）は skill 化すれば層2 で配れる**が、**常時ロードの rule は skill 化・`@import` 迂回いずれでも層2 に乗らない**（前者は skill に常時ロードが無いため、後者は import がテキスト展開で skill 機構を殺し glob 非対応・plugin パス不安定なため。詳細は [§解決案](#resolve) 層2系・出典 [\[68\]](#ref-68)〜[\[70\]](#ref-70)）。常時 rule の中央配布は層3 managed（`claudeMd`）が正規ルート。
- 案A のテンプレート「更新伝播」を仕組み化する標準手段（同期 bot 等）は公式範囲外。
- file-based `managed-settings.json`（MDM/plist/registry 配置）のプラン非依存は、ファイル機構の性質からの**推定**で原文に明示なし。なお **server-managed（admin console 経由）は Claude for Teams/Enterprise が必須**である点は原文明記（出典 [\[49\]](#ref-49)）で、本推定は file-based に限定される。
- `--settings` で渡した settings が、project/local 限定の security 系キー（`defaultMode: auto`／`skipDangerousModePermissionPrompt`／`autoMode` 等）を有効化できるかは原文に明示なし。`--settings` は command-line precedence だが、これらキーの「特定スコープでは無視」という判定が `--settings` 経由でどう働くかは不記載（出典 [\[21\]](#ref-21) [\[62\]](#ref-62)）。
- monorepo 設計（per-directory `CLAUDE.md` / `claudeMdExcludes`（managed `CLAUDE.md` は除外不可）／層化が破綻したら内部 marketplace の plugin に集約）は大規模 repo 向けの参考情報（出典 [\[51\]](#ref-51)）。本問の「ゼロから作る最小リポジトリ」では主対象外。

### 網羅性監査の範囲（「記載漏れ」検出のための逆引き照合）

「漏れ」は記載が無く裏取りの起点が無いため、**source 側の "列挙表（完全集合を定義する表）" → 本書** の方向で差分照合した。照合済み・未照合の範囲を明示する。

- **照合済み**: claude-directory のファイル一覧・"What's not shown"（出典 [\[2\]](#ref-2) [\[3\]](#ref-3)）／plugins-reference の plugin コンポーネント配置表（出典 [\[4\]](#ref-4)）／settings の "Managed settings only" 注記キー群（出典 [\[28\]](#ref-28) ほか 33052-33230 帯）。→ v2.3 で `CLAUDE.local.md`、v2.4 で managed 専用キー 6 件と global/個人用ファイルの取りこぼしを補完。
- **未照合（残リスク）**: plugin-marketplaces の全オプション／hooks・env-vars・statusline 等の全項目は、本書スコープ外項目が大半のため完全照合していない。これらに配布統制上重要な抜けが残る可能性は否定できない。

---

## 次アクション候補

- [ ] 3 チャネル構成の実ディレクトリ構造とテンプレート `settings.json`（`source:'settings'` or `extraKnownMarketplaces`+`enabledPlugins` 込み）の雛形を起こす
- [ ] 実装スコープの決定（層1のみ / 層1+2 / 層1+2+3、IT 統制有無で層3の採否）

---

<a id="sources"></a>

## 出典一覧（v2.1.165 版 llms-full.txt の行番号）

各番号は本文中の `[n]` からアンカーリンクされる。

| # | 主張 | ページ | 行 |
|---|---|---|---|
| <a id="ref-1"></a>1 | `.claude/` は optional（"The rest of the directory is optional"） | claude-directory | 8741 |
| <a id="ref-2"></a>2 | `.claude/` ファイル一覧・commit 可否表（workflows/.worktreeinclude/themes 含む） | claude-directory | 8794-8811 |
| <a id="ref-3"></a>3 | plugin 以外の関連ファイル（managed-settings.json / CLAUDE.local.md / installed plugins） | claude-directory | 8753-8757 |
| <a id="ref-4"></a>4 | plugin コンポーネント配置表（skills/commands/agents/output-styles/themes/hooks/MCP/LSP/monitors/bin/settings） | plugins-reference | 62099-62112 |
| <a id="ref-5"></a>5 | CLAUDE.md は plugin context 非ロード（代替=skill） | plugins-reference | 62094-62095 |
| <a id="ref-6"></a>6 | plugin settings.json は agent/subagentStatusLine のみ | plugins-reference | 62112 |
| <a id="ref-7"></a>7 | channels は MCP ベース（会話へコンテンツ注入。plugin init --with channel） | plugins-reference | 61870-61880, 62157 |
| <a id="ref-8"></a>8 | plugin はキャッシュへ独立コピー・外部参照不可（symlink 例外あり） | plugin-marketplaces | 28642-28645 |
| <a id="ref-9"></a>9 | Standalone vs Plugins・plugin.json 任意 | plugins / plugins-reference | 29614-29617 / 62709-62710 |
| <a id="ref-10"></a>10 | `--scope project` が settings.json(enabledPlugins) に記録 | plugins-reference | 62193 / 61648-61659 |
| <a id="ref-11"></a>11 | command が `/plugin:name` 名前空間化 | plugins | 29616 |
| <a id="ref-12"></a>12 | enabledPlugins + extraKnownMarketplaces の commit 配布 | plugin-marketplaces | 29071-29099 |
| <a id="ref-13"></a>13 | enabledPlugins は trust 時に install を促すプロンプト | discover-plugins / plugin-marketplaces | 13249-13268 / 29073 |
| <a id="ref-14"></a>14 | committed enabledPlugins は fresh machine で install 要 | (changelog) | 3402 |
| <a id="ref-15"></a>15 | `source:'settings'` インライン宣言 | settings | 33576, 33584-33606 |
| <a id="ref-16"></a>16 | `@skills-dir` / `plugin init` / 制約（monitors無効・trust必須・サブディレクトリ非検出・個人スコープのみscaffold） | plugins / plugins-reference | 29772 / 61663-61692, 62120 |
| <a id="ref-17"></a>17 | `CLAUDE_CODE_PLUGIN_SEED_DIR` / 複数seed / CACHE_DIR | plugin-marketplaces | 29105-29139 |
| <a id="ref-18"></a>18 | `defaultEnabled` / `strict:false` / release channel | plugin-marketplaces | 28744 / 29004-29016 / 29253-29324 |
| <a id="ref-19"></a>19 | private repo 認証トークン（GITHUB/GITLAB/BITBUCKET） | plugin-marketplaces | 29044-29048 |
| <a id="ref-20"></a>20 | monorepo 配布（git-subdir / --sparse / npm registry） | plugin-marketplaces | 28861-28898, 29419, 28927 |
| <a id="ref-21"></a>21 | project で無視される security キー（defaultMode:auto 等） | settings | 33183-33185, 33064, 33140 |
| <a id="ref-22"></a>22 | trust dialog 後に skills allowed-tools 有効 | skills | 34885 |
| <a id="ref-23"></a>23 | 配列設定の結合＋重複除去マージ | settings | 33432 |
| <a id="ref-24"></a>24 | rules はシンボリックリンク可 | memory | 24956 |
| <a id="ref-25"></a>25 | `--add-dir` 例外ロード表 / additionalDirectories は skills も非ロード | permissions | 28040-28044 / 34680 |
| <a id="ref-26"></a>26 | `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` | memory | 22544, 24885 |
| <a id="ref-27"></a>27 | managed `claudeMd` / OS-path CLAUDE.md（全セッション強制ロード） | settings / memory | 33073 / 24982-25013 |
| <a id="ref-28"></a>28 | `allowManagedPermissionRulesOnly`/`allowManagedHooksOnly`/`allowManagedMcpServersOnly` 等 | settings / permissions | 33056-33058 / 28080-28082 |
| <a id="ref-29"></a>29 | `strictPluginOnlyCustomization` | settings / permissions | 33134, 33857 / 28090 |
| <a id="ref-30"></a>30 | `strictKnownMarketplaces` 値セマンティクス / exact-match 非正規化 | plugin-marketplaces / settings | 29147-29232 / 33625, 33773 |
| <a id="ref-31"></a>31 | `blockedMarketplaces` | settings | 33071 |
| <a id="ref-32"></a>32 | `requiredMinimumVersion`/`requiredMaximumVersion` | settings / admin-setup | 33119-33120 / 79 |
| <a id="ref-33"></a>33 | `forceLoginMethod`/`forceLoginOrgUUID` / `policyHelper` / `forceRemoteSettingsRefresh` / `pluginSuggestionMarketplaces` / `parentSettingsBehavior` | settings | 33095-33097 / 33110, 33113, 33115, 33379 |
| <a id="ref-34"></a>34 | managed 4層・優先・先勝ち非マージ / `wslInheritsWindowsSettings` | admin-setup / server-managed-settings / settings | 42-49 / 32664 / 33412, 33145 |
| <a id="ref-35"></a>35 | `managed-settings.d/` ドロップイン / MDM starter templates | settings | 32984-32992 |
| <a id="ref-36"></a>36 | Windows パス変更（ProgramData 廃止 v2.1.75） | settings | 32974-32982 |
| <a id="ref-37"></a>37 | managed settings は上書き不可 | claude-directory / server-managed-settings | 8755 |
| <a id="ref-38"></a>38 | skills は Enterprise スコープで配布 | skills | 34637 |
| <a id="ref-39"></a>39 | managed subagents（managed dir の .claude/agents/・最高優先）/ plugin subagent は hooks/mcpServers/permissionMode 無視 | sub-agents | 36885, 36890 |
| <a id="ref-40"></a>40 | enabledPlugins を managed で force-enable / 全スコープ install ブロック | settings | 33518 |
| <a id="ref-41"></a>41 | enabledPlugins は `--add-dir` で読まれる | permissions | 28043, 5500 |
| <a id="ref-42"></a>42 | output-styles は plugin/commit 可 | plugins-reference | 62105 |
| <a id="ref-43"></a>43 | themes は global only・commit不可 / plugin では experimental | claude-directory / plugins-reference | 8811 / 62105 |
| <a id="ref-44"></a>44 | workflows/*.js は commit 可 | claude-directory | 8806 |
| <a id="ref-45"></a>45 | agent-memory（project）は VCS 共有可 | sub-agents | 37127 |
| <a id="ref-46"></a>46 | managed-mcp.json は server-managed では配布不可・キーで代替 | server-managed-settings | 32655 |
| <a id="ref-47"></a>47 | `/status` の Setting sources で確認 | settings | 33437 |
| <a id="ref-48"></a>48 | server-managed の third-party provider バイパス | server-managed-settings | 32743 |
| <a id="ref-49"></a>49 | server-managed settings は Claude for Teams（v2.1.38+）/ Enterprise（v2.1.30+）で利用可（"available for Claude for Teams and Claude for Enterprise"・device management 不要） | server-managed-settings | 32543, 32550-32551 |
| <a id="ref-50"></a>50 | skills/subagents 配布3経路 / skillOverrides | skills / sub-agents | 35109 / 36826 / 35084 |
| <a id="ref-51"></a>51 | monorepo per-dir CLAUDE.md / claudeMdExcludes / 内部marketplace集約 | large-codebases | 22322-22408, 22619 |
| <a id="ref-52"></a>52 | `CLAUDE.md` は `./CLAUDE.md` または `./.claude/CLAUDE.md` に配置可（全発見ファイルを連結ロード） | memory | 24784, 24793 / 61012 |
| <a id="ref-53"></a>53 | `CLAUDE.local.md`（project root・個人用・`.gitignore`・`CLAUDE.md` と併読される正規ファイル） | claude-directory / memory | 8756 / 24832 |
| <a id="ref-54"></a>54 | `channelsEnabled`（組織で channels を許可。Managed settings only） | settings | 33072 |
| <a id="ref-55"></a>55 | `allowedChannelPlugins`（channel plugin allowlist。Managed settings only） | settings | 33053 |
| <a id="ref-56"></a>56 | `pluginTrustMessage`（plugin trust 警告への組織メッセージ。Managed settings only） | settings | 33114 |
| <a id="ref-57"></a>57 | `allowAllClaudeAiMcps`（managed-mcp.json 併用時の claude.ai connectors 許可。Managed settings only） | settings | 33052 |
| <a id="ref-58"></a>58 | `filesystem.allowManagedReadPathsOnly` / `network.allowManagedDomainsOnly`（read パス・ドメインを managed のみに。Managed settings only） | settings | 33217 / 33224 |
| <a id="ref-59"></a>59 | `sshHostAllowlist`（Desktop アプリ専用）/ `bwrapPath`・`socatPath`（Linux/WSL2 サンドボックス binary。いずれも Managed settings only） | settings | 12209-12234 / 33229-33230 |
| <a id="ref-60"></a>60 | skill は `SKILL.md`（必須エントリポイント）を持つディレクトリ。任意の補助ファイル（templates / examples / scripts / reference docs）を同梱し `SKILL.md` から参照する（"Add supporting files"）。commands はフラット単一ファイルで補助ファイル不可 | skills | 34660-34672, 34812-34834 / 34548, 34675 |
| <a id="ref-61"></a>61 | hook が実行するスクリプトの配置・参照: project は `$CLAUDE_PROJECT_DIR` 相対（例 `scripts/`・`.claude/hooks/`）／global は `~/.claude/hooks/`／plugin は `scripts/` を `${CLAUDE_PLUGIN_ROOT}` で参照 | hooks / plugins-reference | 8149, 11126 / 62083 |
| <a id="ref-62"></a>62 | settings precedence の完全順序（1.Managed〔command line でも上書き不可〕 2.Command line arguments 3.Local project 4.Shared project 5.User） | permissions | 28099-28105 |
| <a id="ref-63"></a>63 | `enabledPlugins` の format（`"plugin@marketplace": true/false`）とスコープ（user/project/local/managed）・project が user に優先する note ／ `extraKnownMarketplaces` 定義 | settings | 33509-33536 / 33538-33568 |
| <a id="ref-64"></a>64 | user スコープの `enabledPlugins`/plugin はチーム配布されない（commit 共有対象外・web セッション非対応／全員へは project の checked-in settings に宣言） | claude-directory / plugins | 7984 / 32332-32340 |
| <a id="ref-65"></a>65 | `extraKnownMarketplaces` の `autoUpdate`（起動時 refresh。公式 marketplace は default `true`・他は default `false`／managed で `autoUpdate:true` 強制可） | settings / discover-plugins | 33582 / 13234-13236 |
| <a id="ref-66"></a>66 | `--settings <file-or-json>` フラグ（JSON ファイル/文字列ロード。automation/CI・agent view/background・Agent SDK 向けの per-invocation／セッション限り override） | (changelog) / agent-view / auto-mode-config / claude-directory | 6747 / 983 / 2037 / 8786 |
| <a id="ref-67"></a>67 | `settings.json` にファイル内 import 機能は無い／`CLAUDE.md` は `@path` import 構文を持つ（memory 専用） | memory | 2375-2378 |
| <a id="ref-68"></a>68 | skill frontmatter の `paths`（glob で活性化を限定。path-specific rules と同 format）／`user-invocable: false`（`/` メニュー非表示）／`disable-model-invocation`（自動ロード抑止） | skills | 34755, 34763 / 34836-34842 |
| <a id="ref-69"></a>69 | rules/skills の使い分け（rules・`paths` 無しは毎セッション context ロード〔`.claude/CLAUDE.md` 同等優先〕／skills は呼出時・関連判断時・`paths` 該当時のみロード） | memory | 24897-24899, 24915 |
| <a id="ref-70"></a>70 | `CLAUDE.md` `@import`：内容を起動時 context へ展開ロード・相対/絶対パス・再帰最大4ホップ・外部 import は承認ダイアログ（単一ファイルパス＝glob 記載なし） | memory | 24817-24843 |
| <a id="ref-71"></a>71 | `--add-dir` ＋ `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` で `<D>/CLAUDE.md`・`<D>/.claude/CLAUDE.md`・`<D>/.claude/rules/*.md`・`<D>/CLAUDE.local.md` をロード | memory | 24881-24891 |
| <a id="ref-72"></a>72 | `/debug [issue]`／"Debug your configuration"（設定・hook・ファイルが効かない時の検査コマンドと症状別 lookup table） | debug-your-config / claude-directory | 11324, 11347 / 8815 |
| <a id="ref-73"></a>73 | 設定・資産を起動時に外部から渡す CLI フラグ群（`--add-dir`/`--settings`/`--mcp-config`/`--strict-mcp-config`/`--plugin-dir`/`--plugin-url`/`--agents`/`--append-system-prompt-file` 等） | cli-reference | 9119-9173 |

---

<a id="appendix-a"></a>

## 付録A：層3（Managed settings）の統制（強制）キー一覧

[§解決案](#resolve) 層3系（案D）の補足資料。本表のキーはすべて **Managed settings only**（managed 設定でのみ有効＝メンバーは上書き不可）。層3 が「統制（強制）」に使えるキーの全件一覧として、視認性のため本文から分離した。

| キー | 効果 | 出典 |
|---|---|---|
| `strictKnownMarketplaces` | 許可リスト（未定義=制限なし / `[]`=完全ロックダウン / リスト=許可制。`hostPattern`/`pathPattern` 正規表現）。**exact match は URL 正規化せず**（末尾 `/`・`.git`・`ssh://`vs`https://` は別物） | [\[30\]](#ref-30) |
| `blockedMarketplaces` | 拒否リスト | [\[31\]](#ref-31) |
| `strictPluginOnlyCustomization` | skills/agents/hooks/MCP を **plugin + managed 由来のみ**に制限（配列で個別指定可。v2.1.82+） | [\[29\]](#ref-29) |
| `allowManagedPermissionRulesOnly` / `permissions.disableBypassPermissionsMode` | permission を managed のみに / `--dangerously-skip-permissions` 無効化 | [\[28\]](#ref-28) |
| `allowManagedHooksOnly` / `allowedHttpHookUrls` | hooks を managed のみに / HTTP hook URL 制限 | [\[28\]](#ref-28) |
| `allowManagedMcpServersOnly` | MCP allowlist を managed のみに | [\[28\]](#ref-28) |
| `requiredMinimumVersion` / `requiredMaximumVersion` | 範囲外バージョンで**起動拒否**（`minimumVersion` より強い。recover ツールは動作。v2.1.163） | [\[32\]](#ref-32) |
| `forceLoginMethod` / `forceLoginOrgUUID` | アカウント種別・所属組織を強制 | [\[33\]](#ref-33) |
| `policyHelper` | デバイス posture 等から managed settings を**動的計算**する実行ファイル（v2.1.136+） | [\[33\]](#ref-33) |
| `forceRemoteSettingsRefresh` | 起動時リモート取得を強制・失敗で起動拒否（フェイルクローズ） | [\[33\]](#ref-33) |
| `pluginSuggestionMarketplaces` | contextual install 提案に出す marketplace を制限 | [\[33\]](#ref-33) |
| `wslInheritsWindowsSettings` | WSL が Windows ポリシーを継承（既定は Linux パスのみ） | [\[34\]](#ref-34) |
| `parentSettingsBehavior` | IDE/SDK host が供給する managed settings の扱い（first-wins / merge。v2.1.133+） | [\[33\]](#ref-33) |
| `channelsEnabled` | 組織で channels（[前提情報 (D)](#prereq-d) のコンポーネント）を許可（claude.ai Team/Enterprise は未設定/`false` で blocked） | [\[54\]](#ref-54) |
| `allowedChannelPlugins` | メッセージ push 可能な channel plugin の allowlist（未定義=Anthropic 公式 allowlist／空配列=全 block。`channelsEnabled:true` 必須） | [\[55\]](#ref-55) |
| `pluginTrustMessage` | plugin インストール前の trust 警告に追記する組織独自メッセージ | [\[56\]](#ref-56) |
| `allowAllClaudeAiMcps` | `managed-mcp.json` 配備時に claude.ai connectors も併せてロード許可 | [\[57\]](#ref-57) |
| `filesystem.allowManagedReadPathsOnly` | read 許可パス（`filesystem.allowRead`）を managed のみに（`denyRead` は全 source マージ） | [\[58\]](#ref-58) |
| `network.allowManagedDomainsOnly` | 許可ドメイン（`allowedDomains`/`WebFetch(domain:)`）を managed のみに | [\[58\]](#ref-58) |

> 一覧から除外した managed 専用キー（漏れではなくスコープ外）: `sshHostAllowlist`（Claude Desktop アプリ専用）、`bwrapPath`/`socatPath`（Linux/WSL2 サンドボックス binary パス）。詳細は [§解決案](#resolve) 層3系の補足（出典 [\[59\]](#ref-59)）。

---

<a id="appendix-b"></a>

## 付録B：設定・資産を渡す起動時 CLI オプション一覧

[§解決案](#resolve) 層1系・パターン2/3 の補足資料。`.claude/` 資産・設定を**起動時に外部から渡す** CLI フラグの一覧（出典 [\[73\]](#ref-73) cli-reference）。**層2 で配れる資産は層2（Plugin/Marketplace）で配るのが原則**で、本オプション群は主に **層2 で配れない資産を共通リポジトリ `<D>` から読み込ませる**（パターン2）ために用いる。

| フラグ | 渡せるもの | 形式 | 本書方針での主用途 |
|---|---|---|---|
| `--add-dir <dir>` | 追加ディレクトリの skills / `enabledPlugins` / `extraKnownMarketplaces`／＋環境変数併用で CLAUDE.md・rules（**限定ロード**・出典 [\[25\]](#ref-25)[\[26\]](#ref-26)） | ディレクトリ | パターン2: `<D>` の CLAUDE.md/rules/skills |
| `--settings <file-or-json>` | settings.json 全般（permissions/hooks/env 等）。command-line precedence で他スコープと**マージ**（出典 [\[62\]](#ref-62)[\[66\]](#ref-66)） | ファイル or JSON | パターン2: `<D>` の permissions |
| `--mcp-config <file-or-json>` | MCP サーバー定義（複数可）。`--strict-mcp-config` で当該のみに限定 | ファイル or JSON | パターン2: `<D>` の MCP |
| `--plugin-dir <dir/.zip>` | **plugin 一式**（skills/commands/agents/hooks/MCP/output-styles/LSP/monitors/bin/channels）。「そのセッションのみ」 | ディレクトリ or .zip | 層2 を marketplace なしで配る代替（本方針では原則不使用 ※） |
| `--plugin-url <url>` | plugin `.zip`（URL から） | URL | 同上 ※ |
| `--agents <json>` | subagent 定義（動的・インライン JSON） | JSON 文字列 | 一時的な subagent 定義 |
| `--append-system-prompt-file <file>` | system prompt への追記テキスト | ファイル | 共通の system prompt 追記 |

> **共通の注意**: これらは原則 **「そのセッションのみ」適用**で、毎起動でランチャーが付与する必要がある（恒久設定ではない）。**強制力は弱い**（ランチャー経由起動・`<D>` 書き換えを各自任せ＝[§推奨](#recommend) の「強制は層3 だけ」と整合）。`--settings`/`--mcp-config` とも **managed policy（層3）には従う**（managed の deny を緩められない）。
>
> **※ `--plugin-dir`/`--plugin-url` について**: plugin 化すれば agents/commands/hooks/output-styles も `<D>` から marketplace なしに配れる（[§解決案](#resolve) 層2系・案B'' の CLI フラグ版に相当）。ただし plugin の制約（`settings.json` は `agent`/`subagentStatusLine` のみ・plugin root の CLAUDE.md 非ロード・subagent の hooks/mcpServers/permissionMode 無視）を引き継ぐため、**CLAUDE.md/rules/permissions は `--plugin-dir` でも運べない**。本書の「層2 で配れるものは層2」方針では、これらは marketplace 経由（案B〜B'''）を主とし、`--plugin-dir` は一時利用・特殊ケース向けに留める。

**ランチャー集約の例**（パターン2 の実体）:

```bash
claude \
  --add-dir <D> \
  --settings <D>/.claude/settings.json \
  --mcp-config <D>/.mcp.json
# ＋ 環境変数 CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1 を export（CLAUDE.md/rules 読込）
```

このランチャースクリプト自体は作業リポジトリ直下で実行する必要があるため、**パターン3**（別リポジトリから手動配置）で配る。

---

## 変更履歴

> 本書は `.claude/workspace/portable-claude-dir-sharing/` での調査（初版〜v2.6）と3観点クロスレビューを経て確定し、reports/ 昇格時に **v1.0 として再採番**した。昇格前の詳細な改訂経緯（v2.0 全面改訂／v2.2〜v2.4 のレビュー反映・記載漏れ網羅性監査／v2.5 の §解決案・§推奨 再構成／v2.6 のクロスレビュー反映 等）は **Git 履歴**および **workspace の凍結スナップショット（v2.0〜v2.6）** を参照。

- **v1.0（2026-06-07）**: 確定版。Claude Code 公式ドキュメント（CLI v2.1.165 相当）の関連11ページ網羅精読に基づき、資産×配布チャネル（層1 Git commit／層2 Plugin・Marketplace／層3 Managed settings）のマトリクスを中核に構成。3観点クロスレビュー（報告書は [`./レビュー/`](./レビュー/)）を反映済み——§エグゼクティブサマリ新設、出典72件（スラッグ誤り訂正・server-managed のプラン要件を Teams/Enterprise へ訂正）、マトリクス凡例整理、「強制力」定義の3段階化、ポータブル最小構成スターター追加 等。作業指示者判断により層2集約方針を維持（ポータビリティとのトレードオフは注記強化）。出典アンカー機械検証パス（REFS=DEFS=72・BROKEN/ORPHAN ゼロ）。
- **v1.1（2026-06-07）**: 起動時 CLI オプションによる配布経路を整理し追補。(1) §解決案 層1系に「**層1 の配布パターン 1/2/3**」（1＝作業リポジトリに commit／2＝共通リポジトリ `<D>` を clone ＋ 起動オプションで読込／3＝別リポジトリから手動で作業リポジトリ直下へ移動）を追記。(2) **付録B：設定・資産を渡す起動時 CLI オプション一覧**（`--add-dir`/`--settings`/`--mcp-config`/`--strict-mcp-config`/`--plugin-dir`/`--plugin-url`/`--agents`/`--append-system-prompt-file`）を新設し、出典 [73]（cli-reference）を追加。(3) `(C)` の `.worktreeinclude` を「worktree 運用時のみ必要・foreground orchestration 中心の本方針では配布対象外」と明記。出典 72→73。アンカー検証パス（REFS=DEFS=73・BROKEN/ORPHAN ゼロ）。
