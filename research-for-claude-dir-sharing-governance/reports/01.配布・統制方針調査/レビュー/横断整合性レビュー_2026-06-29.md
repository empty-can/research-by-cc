# 横断整合性レビュー報告書（2026-06-29）

> 成果物群を横断して **(1) 公式 docs 最新版に対する陳腐化** と **(2) 文書間の整合性（事実矛盾・参照・用語・テンプレ乖離・論理/実用性）** を点検し、検出した不整合を修正した記録。レビューは複数の SubAgent を並列起動して観点分担で実施した。

## 0. スコープ・方法

- **対象**: 主要成果物＋テンプレ＋公開資料
  - `結論・構成案_…_v1.2.md`（中核）／`01.Plugin・Marketplace編` 調査結果・手順書／`02.Marketplace外資産編` 調査結果・手順書／`03.実装テンプレート（層1+2）`（README＋`layer1-repo-template/`＋`layer2-plugin/`）／フォルダ直下 公開 `README.md`／`CLAUDE.md`（索引）
  - 過去のクロスレビュー報告書3本（`./`）は **参照のみ**（指摘対象外）
- **照合元（公式 docs）**: `code.claude.com/docs/llms-full.txt`（**v2.1.195 相当・2026-06-28 スナップショット**）。報告書本文の出典基準は v2.1.165 のため、版差は陳腐化照合で扱う。
- **方法**: 観点ごとに SubAgent を並列起動（陳腐化照合7本／整合性5観点）。各 Agent は read-only で指摘のみを返し、修正の採否はメインで裁定・実ファイルで裏取りのうえ適用。

## 1. 前段: 公式 docs 最新版への陳腐化照合

調査レポート本体（v1.2／2 調査結果／2 手順書）は **陳腐化 0 件**。版依存箇所（`--add-dir`×subagents の版境界・ProgramData 廃止 v2.1.75・plugin cache/data・managed キー・settings 優先順位）は現行 docs と整合。修正は主に **配布テンプレートの無効スキーマ・内部不整合** に集中した。

| 適用 | ファイル | 内容 |
|---|---|---|
| ✅ | `layer1.../README.md.example` | `sandbox: true` → `"sandbox": { "enabled": true }`（boolean は無効スキーマ） |
| ✅ | `layer1.../.claude/settings.json` | `WebFetch(domain:github.com/anthropics)` → `WebFetch(domain:github.com)`（`domain:` はホスト名のみマッチ。パス付きは死にルール。※全 github.com 許可へ広がる点は許容判断） |
| ✅ | `layer1.../CLAUDE.md.example` | install コマンドの marketplace 名を `base-dev-kit@base-dev-kit-marketplace` に修正（内部不整合） |
| ✅ | `layer2.../plugin.json` | `$schema` を docs 例 `claude-code-plugin-manifest.json` に統一（load 時無視・エディタ補完用） |
| ✅ | `02.../Marketplace外資産_手順書.md` | 「`settings.local.json` は `--add-dir` でも読まれない」を 2 キー例外ありに精密化（docs L26257） |

> 報告のみ（未変更）: `/reload-skills`（v2.1.157 新設）への言及追補余地／`.zip` 対応の導入版番号 `v2.1.128` の独立確認不可／v1.2 付録A の managed キー網羅性（モデル制限系はスコープ外）／出典行番号のドリフト（v2.1.165 基準と明示済み）。

## 2. 横断整合性レビュー（5 観点）

| 観点 | 件数 | 結果 |
|---|---|---|
| A. 文書間の事実矛盾＋論理破綻 | 3 | F1・F2・F4 |
| B. 参照・リンク・§/出典アンカー | **0** | 全リンク・パス・§・アンカー生存。REFS=DEFS=75 を独立再検証 |
| C. 用語・命名の揺れ | 3 | F7・F8・J3 |
| D. テンプレ↔本文の乖離・実用性 | 6 | F3・F4・F5・F6（＋ J2・F8） |
| E. 全体導線の論理・実用性・網羅 | 5 | F1・F2・F9・J1（＋ POSITIVE） |

### 検出と対応（F1〜F9・すべて適用済み）

| # | 深刻度 | 検出（観点） | 対応 |
|---|---|---|---|
| **F1** | 高 | `settings.local.json`×`--add-dir` の可否が **調査結果↔手順書で真逆**（前段の手順書 v1.4 精密化に調査結果が追従せず）。漏洩境界の判断に関わる（A-1/E-1） | `02.Marketplace外資産 調査結果` §2 結合表・§優先順位注記・§6 落とし穴を「2 キー例外あり」に精密化し手順書 v1.4 と一致。調査結果に変更履歴 **v1.4** 追記 |
| **F2** | 中 | `01.Plugin・Marketplace編` だけ subagents×`--add-dir` を **無条件 ✅**（他全文書は版境界付き）。v2.1.165 読者が空振りする（A-2/E-2） | 01 調査結果 §4 表・01 手順書 §2 に「v2.1.178+。v2.1.165 までは非ロード」を付与。両ファイルに変更履歴 **v1.2** 追記 |
| **F3** | 高 | 03/README ツリー注記 `hooks.json # SessionStart（${CLAUDE_PLUGIN_ROOT} 参照）` が実体（インライン `git status --short`）と矛盾。README 自身の別箇所とも食い違い（D-1） | ツリー注記を「インライン git status --short」に修正 |
| **F4** | 中〜高 | **MCP ポリシーの置き場が矛盾**。`.claude/CLAUDE.md`＋`README.md.example` は README 隔離だが `CLAUDE.md.example` は root `CLAUDE.md` に `## MCP ポリシー` を持つ。root `CLAUDE.md` は `--add-dir`+env で漏れ、README 隔離設計を自破り（A-B/D-2） | `CLAUDE.md.example` から MCP ポリシー節を削除し README へ一本化。冒頭コメントも README 隔離方針へ整合 |
| **F5** | 中 | `your-org`/`Your Team`/`team@example.com` の**置換誘導が無く**、コピーすると存在しない repo を参照し層2 が起動しない（D-3） | 03/README「起動装置が『のり』」に**コピー後の置換（必須）**注記を追加 |
| **F6** | 低 | 03/README 振り分け表が skills 4 本を同梱に見せる（実体は `example-skill` 雛形のみ）／README 隔離根拠「`--add-dir` 非ロード」が不正確（README は元来非ロード）（D-5/D-6） | 表セルに「形式雛形のみ同梱」注記／根拠を「memory ファイルでなく `--add-dir`+env でも非ロード」に精緻化 |
| **F7** | 高 | 中核 **v1.2 に旧記号 `<D>` が残存**（§解決案・付録B・出典[71] 等 約20箇所）。後続は全て `<Share>`、手順書 変更履歴は「`<D>` 一掃」と宣言済み（C-1） | v1.2 全体で `<D>` → `<Share>` に置換（意味不変）。変更履歴 **v1.3 補足** 追記 |
| **F8** | 中 | `sub-agents`（03/README・layer1 `.claude/CLAUDE.md`・`README.md.example`）vs `subagents`（他全文書）（C-2） | 当該 3 箇所を `subagents` に統一（v1.2 出典表の docs ページ名 `sub-agents` は固有名なので不変） |
| **F9** | 低 | 索引 `CLAUDE.md` の陳腐化: 公開 README が `- [ ]` のまま（実体はコミット済 d3c766d）／成果物表が全て「v1.0」だが実体は調査結果 v1.3-1.4・手順書 v1.2-1.4（E-4） | チェックボックスを `- [x]` 化・公開 README 行追加・版番号は索引から外し「各ファイル変更履歴参照」に統一（ドリフト防止） |

## 3. 別タスク化・保留

| # | 内容 | 判断 |
|---|---|---|
| **J1** | `--add-dir` 例外ロード表が **6 か所以上に複製**され「正本」不在（F1/F2 の片側更新漏れの構造的原因）（E-3） | **別タスク化**（作業指示者合意）。正本を v1.2 に定め他文書は参照リンクへ寄せる。索引 `CLAUDE.md` タスク進行状況に起票済み |
| **J2** | 層1 `settings.json` の `Read(./.env.*)` deny が、配布対象の `.env.example` も封じる（D-4） | **保留（現状維持推奨）**。deny を狭めると `.env.production` 等の保護が緩む。過剰 deny は安全側でユーザの秘匿方針とも整合 |
| **J3** | 「Plugin・Marketplace」中黒 vs 「Plugin / Marketplace」スラッシュ（C-3） | **保留**。ファイル名は中黒（リネーム不可）。本文のみ統一は低価値 |

## 4. POSITIVE（観点E）

- 方針（v1.2 §マトリクス①②③④）→ 実装（03 テンプレの振り分け表）が根拠付き 1:1 対応。`code-reviewer` が hooks/mcpServers/permissionMode を使わず①△制約をコードで体現。
- 「手順書＝手順／調査結果＝根拠」など各文書が責務と参照先を宣言（今回の矛盾は設計欠陥でなく運用漏れ）。
- 残リスク・版依存を errata と網羅性監査で透明に開示。

## 5. 適用済み変更（ファイル別）

- `reports/01.配布・統制方針調査/結論・構成案_…_v1.2.md` — F7（`<D>`→`<Share>` 全置換）＋変更履歴 v1.3 補足
- `reports/02.配布物の開発・テスト/01.Plugin・Marketplace編/Plugin・Marketplace配布物の開発・テスト_調査結果.md` — F2（§4 表 版境界）＋変更履歴 v1.2
- `reports/02.配布物の開発・テスト/01.Plugin・Marketplace編/Plugin開発・テスト_手順書.md` — F2（§2 版境界）＋変更履歴 v1.2／（前段）§5・§6・§② の陳腐化照合は v1.1 で反映済み
- `reports/02.配布物の開発・テスト/02.Marketplace外資産編/Marketplace外資産の開発・テスト_調査結果.md` — F1（settings.local.json 2 キー例外 ×3）＋変更履歴 v1.4
- `reports/02.配布物の開発・テスト/02.Marketplace外資産編/Marketplace外資産_開発・テスト_手順書.md` —（前段）F1 相当を v1.4 で反映済み
- `reports/03.実装テンプレート（層1+2）/README.md` — F3・F5・F6・F8
- `reports/03.実装テンプレート（層1+2）/layer1-repo-template/README.md.example` — F8／（前段）sandbox スキーマ
- `reports/03.実装テンプレート（層1+2）/layer1-repo-template/.claude/CLAUDE.md` — F8
- `reports/03.実装テンプレート（層1+2）/layer1-repo-template/.claude/settings.json` —（前段）WebFetch ルール
- `reports/03.実装テンプレート（層1+2）/layer1-repo-template/CLAUDE.md.example` — F4／（前段）marketplace 名
- `reports/03.実装テンプレート（層1+2）/layer2-plugin/plugins/base-dev-kit/.claude-plugin/plugin.json` —（前段）`$schema`
- `CLAUDE.md`（索引） — F9（チェックボックス・成果物表・J1 起票）

## 変更履歴

- **v1.0（2026-06-29）**: 初版。公式 docs 最新版（v2.1.195・2026-06-28）への陳腐化照合（7 SubAgent 並列）と横断整合性レビュー（5 観点 SubAgent 並列）を実施。検出 F1〜F9 を適用、J1 を別タスク化・J2/J3 を保留。参照アンカー（観点B）は問題なしを独立再検証。
