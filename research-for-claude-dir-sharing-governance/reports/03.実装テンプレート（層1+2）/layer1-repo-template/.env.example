# 環境変数テンプレート。コピーして .env にリネームし、実値を記入する（.env は .gitignore 対象）。

# GitHub MCP / gh CLI の認証（必要な場合）
GITHUB_TOKEN=

# 層2 plugin を private Marketplace から自動更新する場合の認証（必要な場合）
# GITHUB_TOKEN / GH_TOKEN / GITLAB_TOKEN / GL_TOKEN / BITBUCKET_TOKEN のいずれか
