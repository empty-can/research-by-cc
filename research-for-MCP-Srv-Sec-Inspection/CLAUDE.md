# CLAUDE.md — research-for-MCP-Srv-Sec-Inspection

> このファイルは `research-for-MCP-Srv-Sec-Inspection/` フォルダでの作業指示を受けた時に遅延ロードされる。
> 調査の目的・背景・参照リソースを記載する。ルートの運用ルールは `../CLAUDE.md` を参照。

## 調査の目的

MCP Server の脆弱性（特に、構築環境の個人情報や機密情報の漏洩に繋がるようなもの）を、MCP Server のソースコードのレビューによって調査・判定するための仕組みを調査・確立すること。

## 背景

### 本実行環境における現状の MCP サーバ利用制約

本リポジトリの実行環境（および利用者の所属組織の業務環境）では、**情報セキュリティ保護上の制約により、MCP サーバの利用が現状正式には認められておらず、一切セットアップされていない**。

この制約は MCP サーバの仕組み自体（Claude Code とサードパーティ製サーバ間の双方向通信、サーバ側コードの任意実行性、トークン・認証情報・ローカルファイルへのアクセス可能性）が、構築環境の個人情報や機密情報の漏洩経路となる潜在リスクを持つことに起因する。

### 本ブランチが達成しようとしているゴール

情報セキュリティ所管部署に対して、以下を満たした上で MCP サーバの利用許諾を得られるようにすること:

1. **しっかりとした審査基準を設けた上で**、
2. **対象 MCP サーバのソースコードレビュー（インスペクション）を実施し**、
3. **充分な確認を行ったものに限って利用する**

という運用フローを確立し、それによって「利用にリスクは無い」と **説得力と自信を持って所管部署に回答・説明できる状態** を目指す。

そのために本調査では以下を整備する:
- MCP サーバのソースコードレビューによる脆弱性（特に構築環境の個人情報・機密情報漏洩に繋がるもの）の調査・判定の仕組み
- 判定基準・観点・チェックリスト
- 実施プロセスと成果物フォーマット

## スコープ

### 含む

（後続の作業で記載予定）

### 含まない

（後続の作業で記載予定）

## 参照リソース

（後続の作業で追記予定）

## レビュー実施ルール

本調査における成果物のレビューは以下のルールに従う。

> **クロスレビュー関連ファイルの参照構成（v3.0α-r1 以降）**:
> - `.claude/rules/cross-review-runtime.md`（path-scoped rule）— レビュー報告書ファイル（`**/レビュー/**/*.md`）またはクロスレビューテンプレ（`.claude/templates/cross-review/*.md`）を読み書きするときに **自動ロード**。セルフレビュー運用ルール（前半 5 回 / 後半 Opus 3 回・Sonnet 4 回 / レビューイ 3 巡 / Round 3 上限なし）・レビューア確認セクション仕様・指摘記載ルール・判断依頼サマリ運用ルール・件数表運用ルール等の詳細運用仕様を集約
> - `.claude/templates/cross-review/README.md`（v3.0α）— テンプレート 3 種の概観・使い方・基本ルール（指摘ラベル定義・態度区分・版数管理基本）。テンプレ参照時に併せて読む
> - `research-for-MCP-Srv-Sec-Inspection/ドキュメント執筆ガイドライン.md`（v2.0）— 執筆段階の予防的ガイドライン。レビュー時にも判断基準の一貫性確保のため参照
>
> 本節の記載は Level 2（本プロジェクト固有）の上書きとして機能する。サブフォルダ `CLAUDE.md` が存在する場合はそちらが Level 3 として最優先される。`/init` 等で CLAUDE.md を作成する際は、上書きが必要なルールがあれば「レビュー実施ルール」節に明示すること。
>
> **v3.0α 暫定運用中**: テンプレート v3.0α は詳細化途上のリリース。L3 該当項目（指摘記載内容フォーマット移行・除外基準運用詳細・件数表セクション再構成）は運用しながら詰めて v3.1 で正式化する方針。既存資産は機械的置換せず、新規執筆ドキュメントから v3.0α を適用する。

### レビュー繰り返し
- Claude Code 単独での **セルフレビュー / クロスレビュー** は、**新規 NG 検出がゼロ** になるまで繰り返し実施する
- 多段構成（例: セルフレビュー → 別 Agent クロスレビュー）の場合、**各段で全 NG への対応を完了した状態で次段へ進む**

### 必須レビュー（作業指示者承認を兼ねる）
以下の成果物は **作業指示者によるレビュー** を必須とする:
- 計画書（v1.0 完成時）
- 各フェーズ完了時の報告書
- 最終報告書

### 個別レビューの依頼
以下の場合は、必須レビュー以外でも作業指示者に個別にレビューを依頼する:
- セルフ／クロスレビューで作業指示者の判断を仰ぐべき指摘が検出された場合
- NG が検出されていなくても、判断ミスによる後続工程への大きな手戻り懸念がある場合

レビュー対象は報告書である必要はなく、**相談用ドキュメント** または **GitHub Issue** でも可。判断が必要な事項を分類する場合は **「課題」** という分類名を使用する。

## ハマりどころ.md 参照ルール

本調査では、MCP Server のセキュリティレビュー手法の確立・運用上の既知の落とし穴・対処法を `research-for-MCP-Srv-Sec-Inspection/ハマりどころ.md` に一元管理する。

### 参照ルール
- **各フェーズ作業の最初に `ハマりどころ.md` を必ず参照する**。
  - 既知のハマりどころに該当しないかを最初に確認し、該当する場合は事前に回避策を組み込む。

### 追記ルール
- **新たに発見したハマりどころは、作業中または作業終了時に `ハマりどころ.md` に追記する**。
- 追記フォーマット・分類セクションは `ハマりどころ.md` 冒頭の運用ルール節を参照する。

## Issue 運用ルール

本調査における GitHub Issue の運用は `research-for-MCP-Srv-Sec-Inspection/Issue運用ルール.md` を参照する。

- 適用範囲: 将来解決必須課題 / 解決不能課題 / 申し送り事項のみ
- 必須ラベル: `backlog`
- フェーズ内課題は Issue 化対象外（従来通り報告書または別紙）

## プロジェクト管理ファイル

| ファイル | 用途 |
|---|---|
| `ハマりどころ.md` | 既知の落とし穴・対処法（詳細は「ハマりどころ.md 参照ルール」節） |
| `Issue運用ルール.md` | GitHub Issue 運用ルール（詳細は「Issue 運用ルール」節） |
| `improvements/【本紙】要改善点一覧.md` | フェーズ別振り返りで識別した Claude 実作業の要改善点（各詳細は `improvements/` フォルダ内別紙。横断スコープ項目はルート CLAUDE.md または `.claude/rules/` に昇格候補） |

## タスク進行状況
<!--
reports/<タスク名>/ 配下のタスクの進捗を簡潔に記録する。
タスクが増えてきたら表形式に切り替えてもよい。
-->

（後続の作業計画策定時に記載予定）

## 変更履歴

- 2026-05-11: 初版作成（`research-for-local-RAG-for-cc/CLAUDE.md` の運用ルール節を流用、調査の目的を MCP Server セキュリティレビューに置換、RAG 固有の背景・スコープ・参照リソース・タスク進行状況は本調査向けに後続記載予定として空欄化、code-reviewer agent 不使用ルールは本調査ではソースコードレビューが中核のため削除）
- 2026-05-11: 「背景」節に、本実行環境における現状の MCP サーバ利用制約と、本ブランチが達成しようとしているゴール（審査基準＋ソースコードインスペクション＋充分確認による安全な MCP サーバ利用許諾フローの確立）を追記
